// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: agentfleet/v1/dashboard.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientReadableStream,
  type ClientUnaryCall,
  type handleServerStreamingCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import {
  GetTaskRequest,
  GetTaskResponse,
  JournalEntry,
  QueryLogsRequest,
  QueryLogsResponse,
  SetPermissionModeRequest,
  SetPermissionModeResponse,
  Task,
} from "./core.js";
import {
  DeleteFileRequest,
  DeleteFileResponse,
  GetFileDownloadUrlRequest,
  GetFileDownloadUrlResponse,
  GetFileUploadUrlRequest,
  GetFileUploadUrlResponse,
  ListFilesRequest,
  ListFilesResponse,
} from "./files.js";
import {
  DeleteWorktreeRequest,
  DeleteWorktreeResponse,
  ListWorktreesRequest,
  ServiceIngredient,
} from "./provisioner.js";
import { ReadTranscriptSinceRequest, ReadTranscriptSinceResponse, TranscriptEntry } from "./transcript.js";

export const protobufPackage = "agentfleet.v1";

export interface ListTasksRequest {
  limit: number;
}

export interface ListTasksResponse {
  tasks: Task[];
}

/**
 * Creates a task the same way a Discord /task command does, minus the
 * Discord thread — repo must be a name in the repos table (docs/adr/0028,
 * core/internal/repos), description must be non-empty. The created task has no
 * discord_channel_id/discord_thread_id, which core/internal/discord/
 * session.go's PostToThread already handles as a no-op relay target.
 * snippet_ids optionally names rows from the prompt_snippets table — their
 * text is resolved and joined once, at creation time, into the new task's
 * own guidance column (see PromptSnippet below); empty means the task gets
 * only its own description, no extra guidance.
 */
export interface CreateTaskRequest {
  repo: string;
  description: string;
  snippetIds: string[];
  model?: string | undefined;
}

export interface CreateTaskResponse {
  task?: Task | undefined;
}

/**
 * Streamed directly as `stream TranscriptEntry` on the RPC below — no
 * wrapper response message, unlike the unary GetTranscript (which reuses
 * transcript.proto's ReadTranscriptSinceResponse).
 */
export interface StreamTranscriptRequest {
  taskId: string;
  sinceSeq: number;
}

export interface GetE2eStatusRequest {
  taskId: string;
}

export interface GetE2eStatusResponse {
  status: string;
  previewUrl: string;
  /**
   * Fields 3-7 are pass-through from ProvisionerService.GetE2eSessionStatus
   * (live pod truth — core holds no cluster RBAC and must not read pods);
   * 8-11 are core's own, resolved from repo_profiles.
   */
  startCmd: string;
  podPhase: string;
  appReady: boolean;
  restarts: number;
  /** RFC3339 */
  startedAt: string;
  profileName: string;
  tools: string[];
  services: string[];
  /**
   * True when the running pod's start_cmd differs from the profile's — i.e.
   * a human-approved per-task override is in effect. Surfacing this is the
   * point: a silent override is what caused the bug this card exists for.
   */
  startCmdOverridden: boolean;
}

export interface KillRequest {
  taskId: string;
  reason?: string | undefined;
}

export interface KillResponse {
  status: string;
}

/**
 * Interrupt stops only the current turn (the in-flight API exchange/tool
 * call) via the SDK's own q.interrupt() — unlike Kill, the session and pod
 * survive and stay ready for the next message. Posted as a distinct
 * "interrupt" transcript entry (never "abort") so the worker can tell the
 * two apart and only Kill tears the whole session down.
 */
export interface InterruptRequest {
  taskId: string;
}

export interface InterruptResponse {
  status: string;
}

/**
 * Warm boots a pod for an idle session on demand — the sessions redesign's
 * "bus" model (supersedes docs/adr/0021/0025's phase-boundary framing): a
 * task's pod is ephemeral compute, not tied to the task's whole lifetime.
 * Deliberately does not touch tasks.status (a loose UI-freshness signal
 * now, not control flow) — session state is the worktree + saved
 * session_id, both of which already survive teardown. Rejected with
 * CodeFailedPrecondition if the task already has a live pod (idempotent
 * double-click guard) or the fleet is already at MAX_IN_FLIGHT_TASKS.
 */
export interface WarmRequest {
  taskId: string;
}

export interface WarmResponse {
  status: string;
  podName: string;
}

/**
 * Answers a pending PERMISSION_REQUEST-type transcript entry (posted by
 * canUseTool for any tool call the SDK's current permission mode would
 * prompt for — supersedes docs/adr/0021's Write/Edit-absent-from-
 * allowedTools gate). `seq` is that entry's own seq, carried the same way
 * AnswerQuestion's is — for the dashboard's own bookkeeping, not required
 * for correlation server-side. A sibling RPC to AnswerQuestion rather than
 * overloading it: the payload shape differs (allow/deny/updatedInput vs.
 * free-form answers JSON) and there's no real code to share beyond one
 * AppendReply call either way.
 */
export interface RespondToPermissionRequest {
  taskId: string;
  seq: number;
  decisionJson: string;
}

export interface RespondToPermissionResponse {
  status: string;
}

export interface KillE2eRequest {
  taskId: string;
  /**
   * also_teardown_services (docs/adr/0034 follow-up): also delete the
   * repo's shared postgres/redis instances if this task used any —
   * human-confirmed opt-in, since those are shared across every task
   * against the repo, not owned by this one task alone.
   */
  alsoTeardownServices: boolean;
}

export interface KillE2eResponse {
  killed: boolean;
  servicesTornDown: string[];
}

/**
 * Answers a pending QUESTION-type transcript entry (posted by the agent's
 * AskUserQuestion MCP tool call, see docs/adr/0018). `seq` is that entry's
 * own seq — carried for the dashboard's own bookkeeping/idempotency, not
 * required for correlation server-side (only one question is ever pending
 * per task at a time, since the agent's tool call blocks on it).
 * `answers_json` is a JSON-encoded {"answers": {"<question>": "<label>"}}
 * payload, opaque to fleet-core — it's appended verbatim as the answer
 * entry's `text` and returned verbatim to the blocked MCP tool call.
 */
export interface AnswerQuestionRequest {
  taskId: string;
  seq: number;
  answersJson: string;
}

export interface AnswerQuestionResponse {
  status: string;
}

/**
 * Discuss lets a human send an arbitrary free-text message from the
 * dashboard, full parity with a Discord thread reply — appended as a
 * "discussion" transcript entry, picked up by the worker's existing
 * streamHumanMessages SSE the same way a Discord reply already is (no
 * worker/sidecar changes needed, it's a plain cursor-based transcript
 * stream, not Discord-specific). `from`/`type` are hardcoded server-side,
 * same pattern as Kill hardcoding "human"/"abort" — the dashboard has no
 * need to expose them.
 */
export interface DiscussRequest {
  taskId: string;
  text: string;
}

export interface DiscussResponse {
  status: string;
}

/**
 * Soft-deletes the task (see db/schema.sql's tasks.deleted_at) after
 * force-tearing down any live worker/e2e pod — unlike Kill, which only
 * posts an "abort" transcript entry the worker pod must be alive and
 * cooperative to react to, this calls ProvisionerService.TearDownSession
 * directly so a wedged/crashed pod doesn't block removal. Doesn't touch
 * `status` — a `done` task stays `done`, it just stops appearing in
 * ListTasks.
 */
export interface DeleteTaskRequest {
  taskId: string;
}

export interface DeleteTaskResponse {
  status: string;
}

/**
 * WorktreeView is provisioner.proto's WorktreeInfo left-joined against
 * `tasks` (reliability-findings.md #2) — task_status/task_error/pr_url
 * are unset, not omitted, when the task row itself no longer exists,
 * which is exactly the orphaned-worktree case this view exists to
 * surface (an inner join would hide it).
 */
export interface WorktreeView {
  taskId: string;
  repo: string;
  branch: string;
  upstreamTrack: string;
  mtimeUnix: number;
  taskStatus?: string | undefined;
  taskError?: string | undefined;
  prUrl?: string | undefined;
}

export interface ListWorktreesViewResponse {
  worktrees: WorktreeView[];
}

/**
 * GetJournal is the read path reliability-findings.md #1/#7 both call out
 * as missing — knowledge_journal had no Get/List RPC anywhere, so even a
 * journaled crash event was invisible without direct Postgres access.
 * Typed request/response, same pull/cursor shape as GetTranscript, not a
 * generic Query(bytes) returns (bytes) dispatcher — two concrete gaps
 * don't justify throwing away protobuf's type safety for a general one.
 * JournalEntry itself now lives in core.proto (imported above) — reused by
 * SearchJournal too, same pattern as Task/GetTaskRequest/GetTaskResponse.
 */
export interface GetJournalRequest {
  /** "" matches every repo */
  repo: string;
  sinceId: number;
  limit: number;
}

export interface GetJournalResponse {
  entries: JournalEntry[];
  nextId: number;
}

/**
 * Repo is the dashboard-editable target-repo config (docs/adr/0028) —
 * replaces the hardcoded tasks.KnownRepos Go map, backed by the `repos`
 * table (db/schema.sql). No timestamps exposed: the dashboard's repo list
 * is small and unordered, unlike Task/JournalEntry which need cursor state.
 */
export interface Repo {
  name: string;
  url: string;
  /** "" means the provisioner defaults to "main" */
  baseBranch: string;
}

export interface ListReposRequest {
}

export interface ListReposResponse {
  repos: Repo[];
}

export interface CreateRepoRequest {
  name: string;
  url: string;
  baseBranch: string;
}

export interface CreateRepoResponse {
  repo?: Repo | undefined;
}

export interface UpdateRepoRequest {
  name: string;
  url: string;
  baseBranch: string;
}

export interface UpdateRepoResponse {
  repo?: Repo | undefined;
}

export interface DeleteRepoRequest {
  name: string;
}

export interface DeleteRepoResponse {
  status: string;
}

/**
 * RepoProfile is a dashboard-editable named environment recipe for a repo
 * (docs/adr/0034) — replaces the hardcoded per-repo StartCmdFor switch.
 * Reuses provisioner.proto's ServiceIngredient message (already imported
 * here) rather than redefining it.
 */
export interface RepoProfile {
  repoName: string;
  /** "worker" | "e2e" | "lint" | ... */
  name: string;
  startCmd: string;
  toolKeys: string[];
  serviceIngredients: ServiceIngredient[];
}

export interface ListRepoProfilesRequest {
  repoName: string;
}

export interface ListRepoProfilesResponse {
  profiles: RepoProfile[];
}

export interface CreateRepoProfileRequest {
  repoName: string;
  name: string;
  startCmd: string;
  toolKeys: string[];
  serviceIngredients: ServiceIngredient[];
}

export interface CreateRepoProfileResponse {
  profile?: RepoProfile | undefined;
}

export interface UpdateRepoProfileRequest {
  repoName: string;
  name: string;
  startCmd: string;
  toolKeys: string[];
  serviceIngredients: ServiceIngredient[];
}

export interface UpdateRepoProfileResponse {
  profile?: RepoProfile | undefined;
}

export interface DeleteRepoProfileRequest {
  repoName: string;
  name: string;
}

export interface DeleteRepoProfileResponse {
  status: string;
}

/**
 * PromptSnippet is dashboard-editable, reusable guidance text (same
 * no-redeploy pattern as Repo above), backed by the `prompt_snippets`
 * table (db/schema.sql). An operator optionally attaches a set of these to
 * a task at creation time (CreateTaskRequest.snippet_ids) — replaces
 * worker/src/session.ts's old unconditional, hardcoded workflow prompt. A
 * task's base prompt is just its own description; anything more is one of
 * these, picked per task, not forced on every task regardless of size.
 */
export interface PromptSnippet {
  id: string;
  name: string;
  text: string;
  suggestedPermissionMode?: string | undefined;
}

export interface ListPromptSnippetsRequest {
}

export interface ListPromptSnippetsResponse {
  snippets: PromptSnippet[];
}

export interface CreatePromptSnippetRequest {
  name: string;
  text: string;
}

export interface CreatePromptSnippetResponse {
  snippet?: PromptSnippet | undefined;
}

export interface UpdatePromptSnippetRequest {
  id: string;
  name: string;
  text: string;
}

export interface UpdatePromptSnippetResponse {
  snippet?: PromptSnippet | undefined;
}

export interface DeletePromptSnippetRequest {
  id: string;
}

export interface DeletePromptSnippetResponse {
  status: string;
}

export interface ScheduledAudit {
  id: string;
  name: string;
  prompt: string;
  intervalSeconds: number;
  enabled: boolean;
  nextRunAt: string;
  lastRunAt: string;
  lastStatus: string;
}

export interface ListScheduledAuditsRequest {
}

export interface ListScheduledAuditsResponse {
  audits: ScheduledAudit[];
}

export interface CreateScheduledAuditRequest {
  name: string;
  prompt: string;
  intervalSeconds: number;
}

export interface CreateScheduledAuditResponse {
  audit?: ScheduledAudit | undefined;
}

export interface UpdateScheduledAuditRequest {
  id: string;
  name: string;
  prompt: string;
  intervalSeconds: number;
  enabled: boolean;
}

export interface UpdateScheduledAuditResponse {
  audit?: ScheduledAudit | undefined;
}

export interface DeleteScheduledAuditRequest {
  id: string;
}

export interface DeleteScheduledAuditResponse {
  status: string;
}

export interface ThotEvent {
  id: number;
  kind: string;
  actor: string;
  payload: string;
  replyTo?: number | undefined;
  createdAt: string;
}

export interface ListThotEventsRequest {
  sinceId: number;
  limit: number;
}

export interface ListThotEventsResponse {
  events: ThotEvent[];
  nextId: number;
  /**
   * Requests still awaiting a human decision — sent alongside the feed so
   * a page refresh mid-prompt re-renders the pending card instead of
   * losing it.
   */
  pending: ThotEvent[];
}

export interface RespondToThotPermissionRequest {
  requestId: number;
  allow: boolean;
  /** reason, when denying */
  message: string;
}

export interface RespondToThotPermissionResponse {
  status: string;
}

/**
 * Named DashboardService-prefixed per buf's RPC_REQUEST_STANDARD_NAME rule,
 * since the plain AskThotRequest name is already taken by thot.proto. It
 * carries no asking_task_id — a human is not a task.
 */
export interface DashboardServiceAskThotRequest {
  question: string;
}

export interface DashboardServiceAskThotResponse {
  answer: string;
}

function createBaseListTasksRequest(): ListTasksRequest {
  return { limit: 0 };
}

export const ListTasksRequest: MessageFns<ListTasksRequest> = {
  encode(message: ListTasksRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.limit !== 0) {
      writer.uint32(8).int32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListTasksRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListTasksRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListTasksRequest {
    return { limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0 };
  },

  toJSON(message: ListTasksRequest): unknown {
    const obj: any = {};
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListTasksRequest>, I>>(base?: I): ListTasksRequest {
    return ListTasksRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListTasksRequest>, I>>(object: I): ListTasksRequest {
    const message = createBaseListTasksRequest();
    message.limit = object.limit ?? 0;
    return message;
  },
};

function createBaseListTasksResponse(): ListTasksResponse {
  return { tasks: [] };
}

export const ListTasksResponse: MessageFns<ListTasksResponse> = {
  encode(message: ListTasksResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.tasks) {
      Task.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListTasksResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListTasksResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tasks.push(Task.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListTasksResponse {
    return { tasks: globalThis.Array.isArray(object?.tasks) ? object.tasks.map((e: any) => Task.fromJSON(e)) : [] };
  },

  toJSON(message: ListTasksResponse): unknown {
    const obj: any = {};
    if (message.tasks?.length) {
      obj.tasks = message.tasks.map((e) => Task.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListTasksResponse>, I>>(base?: I): ListTasksResponse {
    return ListTasksResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListTasksResponse>, I>>(object: I): ListTasksResponse {
    const message = createBaseListTasksResponse();
    message.tasks = object.tasks?.map((e) => Task.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCreateTaskRequest(): CreateTaskRequest {
  return { repo: "", description: "", snippetIds: [], model: undefined };
}

export const CreateTaskRequest: MessageFns<CreateTaskRequest> = {
  encode(message: CreateTaskRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.repo !== "") {
      writer.uint32(10).string(message.repo);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    for (const v of message.snippetIds) {
      writer.uint32(26).string(v!);
    }
    if (message.model !== undefined) {
      writer.uint32(34).string(message.model);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateTaskRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateTaskRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.repo = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.snippetIds.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.model = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateTaskRequest {
    return {
      repo: isSet(object.repo) ? globalThis.String(object.repo) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      snippetIds: globalThis.Array.isArray(object?.snippetIds)
        ? object.snippetIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.snippet_ids)
        ? object.snippet_ids.map((e: any) => globalThis.String(e))
        : [],
      model: isSet(object.model) ? globalThis.String(object.model) : undefined,
    };
  },

  toJSON(message: CreateTaskRequest): unknown {
    const obj: any = {};
    if (message.repo !== "") {
      obj.repo = message.repo;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.snippetIds?.length) {
      obj.snippetIds = message.snippetIds;
    }
    if (message.model !== undefined) {
      obj.model = message.model;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateTaskRequest>, I>>(base?: I): CreateTaskRequest {
    return CreateTaskRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateTaskRequest>, I>>(object: I): CreateTaskRequest {
    const message = createBaseCreateTaskRequest();
    message.repo = object.repo ?? "";
    message.description = object.description ?? "";
    message.snippetIds = object.snippetIds?.map((e) => e) || [];
    message.model = object.model ?? undefined;
    return message;
  },
};

function createBaseCreateTaskResponse(): CreateTaskResponse {
  return { task: undefined };
}

export const CreateTaskResponse: MessageFns<CreateTaskResponse> = {
  encode(message: CreateTaskResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.task !== undefined) {
      Task.encode(message.task, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateTaskResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateTaskResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.task = Task.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateTaskResponse {
    return { task: isSet(object.task) ? Task.fromJSON(object.task) : undefined };
  },

  toJSON(message: CreateTaskResponse): unknown {
    const obj: any = {};
    if (message.task !== undefined) {
      obj.task = Task.toJSON(message.task);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateTaskResponse>, I>>(base?: I): CreateTaskResponse {
    return CreateTaskResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateTaskResponse>, I>>(object: I): CreateTaskResponse {
    const message = createBaseCreateTaskResponse();
    message.task = (object.task !== undefined && object.task !== null) ? Task.fromPartial(object.task) : undefined;
    return message;
  },
};

function createBaseStreamTranscriptRequest(): StreamTranscriptRequest {
  return { taskId: "", sinceSeq: 0 };
}

export const StreamTranscriptRequest: MessageFns<StreamTranscriptRequest> = {
  encode(message: StreamTranscriptRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.sinceSeq !== 0) {
      writer.uint32(16).int64(message.sinceSeq);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamTranscriptRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamTranscriptRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sinceSeq = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamTranscriptRequest {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
      sinceSeq: isSet(object.sinceSeq)
        ? globalThis.Number(object.sinceSeq)
        : isSet(object.since_seq)
        ? globalThis.Number(object.since_seq)
        : 0,
    };
  },

  toJSON(message: StreamTranscriptRequest): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.sinceSeq !== 0) {
      obj.sinceSeq = Math.round(message.sinceSeq);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamTranscriptRequest>, I>>(base?: I): StreamTranscriptRequest {
    return StreamTranscriptRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamTranscriptRequest>, I>>(object: I): StreamTranscriptRequest {
    const message = createBaseStreamTranscriptRequest();
    message.taskId = object.taskId ?? "";
    message.sinceSeq = object.sinceSeq ?? 0;
    return message;
  },
};

function createBaseGetE2eStatusRequest(): GetE2eStatusRequest {
  return { taskId: "" };
}

export const GetE2eStatusRequest: MessageFns<GetE2eStatusRequest> = {
  encode(message: GetE2eStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetE2eStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetE2eStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetE2eStatusRequest {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
    };
  },

  toJSON(message: GetE2eStatusRequest): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetE2eStatusRequest>, I>>(base?: I): GetE2eStatusRequest {
    return GetE2eStatusRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetE2eStatusRequest>, I>>(object: I): GetE2eStatusRequest {
    const message = createBaseGetE2eStatusRequest();
    message.taskId = object.taskId ?? "";
    return message;
  },
};

function createBaseGetE2eStatusResponse(): GetE2eStatusResponse {
  return {
    status: "",
    previewUrl: "",
    startCmd: "",
    podPhase: "",
    appReady: false,
    restarts: 0,
    startedAt: "",
    profileName: "",
    tools: [],
    services: [],
    startCmdOverridden: false,
  };
}

export const GetE2eStatusResponse: MessageFns<GetE2eStatusResponse> = {
  encode(message: GetE2eStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    if (message.previewUrl !== "") {
      writer.uint32(18).string(message.previewUrl);
    }
    if (message.startCmd !== "") {
      writer.uint32(26).string(message.startCmd);
    }
    if (message.podPhase !== "") {
      writer.uint32(34).string(message.podPhase);
    }
    if (message.appReady !== false) {
      writer.uint32(40).bool(message.appReady);
    }
    if (message.restarts !== 0) {
      writer.uint32(48).int32(message.restarts);
    }
    if (message.startedAt !== "") {
      writer.uint32(58).string(message.startedAt);
    }
    if (message.profileName !== "") {
      writer.uint32(66).string(message.profileName);
    }
    for (const v of message.tools) {
      writer.uint32(74).string(v!);
    }
    for (const v of message.services) {
      writer.uint32(82).string(v!);
    }
    if (message.startCmdOverridden !== false) {
      writer.uint32(88).bool(message.startCmdOverridden);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetE2eStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetE2eStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.previewUrl = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.startCmd = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.podPhase = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.appReady = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.restarts = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.startedAt = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.profileName = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.tools.push(reader.string());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.services.push(reader.string());
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.startCmdOverridden = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetE2eStatusResponse {
    return {
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      previewUrl: isSet(object.previewUrl)
        ? globalThis.String(object.previewUrl)
        : isSet(object.preview_url)
        ? globalThis.String(object.preview_url)
        : "",
      startCmd: isSet(object.startCmd)
        ? globalThis.String(object.startCmd)
        : isSet(object.start_cmd)
        ? globalThis.String(object.start_cmd)
        : "",
      podPhase: isSet(object.podPhase)
        ? globalThis.String(object.podPhase)
        : isSet(object.pod_phase)
        ? globalThis.String(object.pod_phase)
        : "",
      appReady: isSet(object.appReady)
        ? globalThis.Boolean(object.appReady)
        : isSet(object.app_ready)
        ? globalThis.Boolean(object.app_ready)
        : false,
      restarts: isSet(object.restarts) ? globalThis.Number(object.restarts) : 0,
      startedAt: isSet(object.startedAt)
        ? globalThis.String(object.startedAt)
        : isSet(object.started_at)
        ? globalThis.String(object.started_at)
        : "",
      profileName: isSet(object.profileName)
        ? globalThis.String(object.profileName)
        : isSet(object.profile_name)
        ? globalThis.String(object.profile_name)
        : "",
      tools: globalThis.Array.isArray(object?.tools)
        ? object.tools.map((e: any) => globalThis.String(e))
        : [],
      services: globalThis.Array.isArray(object?.services)
        ? object.services.map((e: any) => globalThis.String(e))
        : [],
      startCmdOverridden: isSet(object.startCmdOverridden)
        ? globalThis.Boolean(object.startCmdOverridden)
        : isSet(object.start_cmd_overridden)
        ? globalThis.Boolean(object.start_cmd_overridden)
        : false,
    };
  },

  toJSON(message: GetE2eStatusResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.previewUrl !== "") {
      obj.previewUrl = message.previewUrl;
    }
    if (message.startCmd !== "") {
      obj.startCmd = message.startCmd;
    }
    if (message.podPhase !== "") {
      obj.podPhase = message.podPhase;
    }
    if (message.appReady !== false) {
      obj.appReady = message.appReady;
    }
    if (message.restarts !== 0) {
      obj.restarts = Math.round(message.restarts);
    }
    if (message.startedAt !== "") {
      obj.startedAt = message.startedAt;
    }
    if (message.profileName !== "") {
      obj.profileName = message.profileName;
    }
    if (message.tools?.length) {
      obj.tools = message.tools;
    }
    if (message.services?.length) {
      obj.services = message.services;
    }
    if (message.startCmdOverridden !== false) {
      obj.startCmdOverridden = message.startCmdOverridden;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetE2eStatusResponse>, I>>(base?: I): GetE2eStatusResponse {
    return GetE2eStatusResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetE2eStatusResponse>, I>>(object: I): GetE2eStatusResponse {
    const message = createBaseGetE2eStatusResponse();
    message.status = object.status ?? "";
    message.previewUrl = object.previewUrl ?? "";
    message.startCmd = object.startCmd ?? "";
    message.podPhase = object.podPhase ?? "";
    message.appReady = object.appReady ?? false;
    message.restarts = object.restarts ?? 0;
    message.startedAt = object.startedAt ?? "";
    message.profileName = object.profileName ?? "";
    message.tools = object.tools?.map((e) => e) || [];
    message.services = object.services?.map((e) => e) || [];
    message.startCmdOverridden = object.startCmdOverridden ?? false;
    return message;
  },
};

function createBaseKillRequest(): KillRequest {
  return { taskId: "", reason: undefined };
}

export const KillRequest: MessageFns<KillRequest> = {
  encode(message: KillRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.reason !== undefined) {
      writer.uint32(18).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KillRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKillRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KillRequest {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : undefined,
    };
  },

  toJSON(message: KillRequest): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.reason !== undefined) {
      obj.reason = message.reason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KillRequest>, I>>(base?: I): KillRequest {
    return KillRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KillRequest>, I>>(object: I): KillRequest {
    const message = createBaseKillRequest();
    message.taskId = object.taskId ?? "";
    message.reason = object.reason ?? undefined;
    return message;
  },
};

function createBaseKillResponse(): KillResponse {
  return { status: "" };
}

export const KillResponse: MessageFns<KillResponse> = {
  encode(message: KillResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KillResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKillResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KillResponse {
    return { status: isSet(object.status) ? globalThis.String(object.status) : "" };
  },

  toJSON(message: KillResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KillResponse>, I>>(base?: I): KillResponse {
    return KillResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KillResponse>, I>>(object: I): KillResponse {
    const message = createBaseKillResponse();
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseInterruptRequest(): InterruptRequest {
  return { taskId: "" };
}

export const InterruptRequest: MessageFns<InterruptRequest> = {
  encode(message: InterruptRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InterruptRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInterruptRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InterruptRequest {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
    };
  },

  toJSON(message: InterruptRequest): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InterruptRequest>, I>>(base?: I): InterruptRequest {
    return InterruptRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InterruptRequest>, I>>(object: I): InterruptRequest {
    const message = createBaseInterruptRequest();
    message.taskId = object.taskId ?? "";
    return message;
  },
};

function createBaseInterruptResponse(): InterruptResponse {
  return { status: "" };
}

export const InterruptResponse: MessageFns<InterruptResponse> = {
  encode(message: InterruptResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InterruptResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInterruptResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InterruptResponse {
    return { status: isSet(object.status) ? globalThis.String(object.status) : "" };
  },

  toJSON(message: InterruptResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InterruptResponse>, I>>(base?: I): InterruptResponse {
    return InterruptResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InterruptResponse>, I>>(object: I): InterruptResponse {
    const message = createBaseInterruptResponse();
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseWarmRequest(): WarmRequest {
  return { taskId: "" };
}

export const WarmRequest: MessageFns<WarmRequest> = {
  encode(message: WarmRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WarmRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWarmRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WarmRequest {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
    };
  },

  toJSON(message: WarmRequest): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WarmRequest>, I>>(base?: I): WarmRequest {
    return WarmRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WarmRequest>, I>>(object: I): WarmRequest {
    const message = createBaseWarmRequest();
    message.taskId = object.taskId ?? "";
    return message;
  },
};

function createBaseWarmResponse(): WarmResponse {
  return { status: "", podName: "" };
}

export const WarmResponse: MessageFns<WarmResponse> = {
  encode(message: WarmResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    if (message.podName !== "") {
      writer.uint32(18).string(message.podName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WarmResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWarmResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.podName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WarmResponse {
    return {
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      podName: isSet(object.podName)
        ? globalThis.String(object.podName)
        : isSet(object.pod_name)
        ? globalThis.String(object.pod_name)
        : "",
    };
  },

  toJSON(message: WarmResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.podName !== "") {
      obj.podName = message.podName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WarmResponse>, I>>(base?: I): WarmResponse {
    return WarmResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WarmResponse>, I>>(object: I): WarmResponse {
    const message = createBaseWarmResponse();
    message.status = object.status ?? "";
    message.podName = object.podName ?? "";
    return message;
  },
};

function createBaseRespondToPermissionRequest(): RespondToPermissionRequest {
  return { taskId: "", seq: 0, decisionJson: "" };
}

export const RespondToPermissionRequest: MessageFns<RespondToPermissionRequest> = {
  encode(message: RespondToPermissionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.seq !== 0) {
      writer.uint32(16).int64(message.seq);
    }
    if (message.decisionJson !== "") {
      writer.uint32(26).string(message.decisionJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RespondToPermissionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRespondToPermissionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.seq = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.decisionJson = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RespondToPermissionRequest {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
      seq: isSet(object.seq) ? globalThis.Number(object.seq) : 0,
      decisionJson: isSet(object.decisionJson)
        ? globalThis.String(object.decisionJson)
        : isSet(object.decision_json)
        ? globalThis.String(object.decision_json)
        : "",
    };
  },

  toJSON(message: RespondToPermissionRequest): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.seq !== 0) {
      obj.seq = Math.round(message.seq);
    }
    if (message.decisionJson !== "") {
      obj.decisionJson = message.decisionJson;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RespondToPermissionRequest>, I>>(base?: I): RespondToPermissionRequest {
    return RespondToPermissionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RespondToPermissionRequest>, I>>(object: I): RespondToPermissionRequest {
    const message = createBaseRespondToPermissionRequest();
    message.taskId = object.taskId ?? "";
    message.seq = object.seq ?? 0;
    message.decisionJson = object.decisionJson ?? "";
    return message;
  },
};

function createBaseRespondToPermissionResponse(): RespondToPermissionResponse {
  return { status: "" };
}

export const RespondToPermissionResponse: MessageFns<RespondToPermissionResponse> = {
  encode(message: RespondToPermissionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RespondToPermissionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRespondToPermissionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RespondToPermissionResponse {
    return { status: isSet(object.status) ? globalThis.String(object.status) : "" };
  },

  toJSON(message: RespondToPermissionResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RespondToPermissionResponse>, I>>(base?: I): RespondToPermissionResponse {
    return RespondToPermissionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RespondToPermissionResponse>, I>>(object: I): RespondToPermissionResponse {
    const message = createBaseRespondToPermissionResponse();
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseKillE2eRequest(): KillE2eRequest {
  return { taskId: "", alsoTeardownServices: false };
}

export const KillE2eRequest: MessageFns<KillE2eRequest> = {
  encode(message: KillE2eRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.alsoTeardownServices !== false) {
      writer.uint32(16).bool(message.alsoTeardownServices);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KillE2eRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKillE2eRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.alsoTeardownServices = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KillE2eRequest {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
      alsoTeardownServices: isSet(object.alsoTeardownServices)
        ? globalThis.Boolean(object.alsoTeardownServices)
        : isSet(object.also_teardown_services)
        ? globalThis.Boolean(object.also_teardown_services)
        : false,
    };
  },

  toJSON(message: KillE2eRequest): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.alsoTeardownServices !== false) {
      obj.alsoTeardownServices = message.alsoTeardownServices;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KillE2eRequest>, I>>(base?: I): KillE2eRequest {
    return KillE2eRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KillE2eRequest>, I>>(object: I): KillE2eRequest {
    const message = createBaseKillE2eRequest();
    message.taskId = object.taskId ?? "";
    message.alsoTeardownServices = object.alsoTeardownServices ?? false;
    return message;
  },
};

function createBaseKillE2eResponse(): KillE2eResponse {
  return { killed: false, servicesTornDown: [] };
}

export const KillE2eResponse: MessageFns<KillE2eResponse> = {
  encode(message: KillE2eResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.killed !== false) {
      writer.uint32(8).bool(message.killed);
    }
    for (const v of message.servicesTornDown) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KillE2eResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKillE2eResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.killed = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.servicesTornDown.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KillE2eResponse {
    return {
      killed: isSet(object.killed) ? globalThis.Boolean(object.killed) : false,
      servicesTornDown: globalThis.Array.isArray(object?.servicesTornDown)
        ? object.servicesTornDown.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.services_torn_down)
        ? object.services_torn_down.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: KillE2eResponse): unknown {
    const obj: any = {};
    if (message.killed !== false) {
      obj.killed = message.killed;
    }
    if (message.servicesTornDown?.length) {
      obj.servicesTornDown = message.servicesTornDown;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<KillE2eResponse>, I>>(base?: I): KillE2eResponse {
    return KillE2eResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<KillE2eResponse>, I>>(object: I): KillE2eResponse {
    const message = createBaseKillE2eResponse();
    message.killed = object.killed ?? false;
    message.servicesTornDown = object.servicesTornDown?.map((e) => e) || [];
    return message;
  },
};

function createBaseAnswerQuestionRequest(): AnswerQuestionRequest {
  return { taskId: "", seq: 0, answersJson: "" };
}

export const AnswerQuestionRequest: MessageFns<AnswerQuestionRequest> = {
  encode(message: AnswerQuestionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.seq !== 0) {
      writer.uint32(16).int64(message.seq);
    }
    if (message.answersJson !== "") {
      writer.uint32(26).string(message.answersJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AnswerQuestionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnswerQuestionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.seq = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.answersJson = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AnswerQuestionRequest {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
      seq: isSet(object.seq) ? globalThis.Number(object.seq) : 0,
      answersJson: isSet(object.answersJson)
        ? globalThis.String(object.answersJson)
        : isSet(object.answers_json)
        ? globalThis.String(object.answers_json)
        : "",
    };
  },

  toJSON(message: AnswerQuestionRequest): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.seq !== 0) {
      obj.seq = Math.round(message.seq);
    }
    if (message.answersJson !== "") {
      obj.answersJson = message.answersJson;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AnswerQuestionRequest>, I>>(base?: I): AnswerQuestionRequest {
    return AnswerQuestionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AnswerQuestionRequest>, I>>(object: I): AnswerQuestionRequest {
    const message = createBaseAnswerQuestionRequest();
    message.taskId = object.taskId ?? "";
    message.seq = object.seq ?? 0;
    message.answersJson = object.answersJson ?? "";
    return message;
  },
};

function createBaseAnswerQuestionResponse(): AnswerQuestionResponse {
  return { status: "" };
}

export const AnswerQuestionResponse: MessageFns<AnswerQuestionResponse> = {
  encode(message: AnswerQuestionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AnswerQuestionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnswerQuestionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AnswerQuestionResponse {
    return { status: isSet(object.status) ? globalThis.String(object.status) : "" };
  },

  toJSON(message: AnswerQuestionResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AnswerQuestionResponse>, I>>(base?: I): AnswerQuestionResponse {
    return AnswerQuestionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AnswerQuestionResponse>, I>>(object: I): AnswerQuestionResponse {
    const message = createBaseAnswerQuestionResponse();
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseDiscussRequest(): DiscussRequest {
  return { taskId: "", text: "" };
}

export const DiscussRequest: MessageFns<DiscussRequest> = {
  encode(message: DiscussRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.text !== "") {
      writer.uint32(18).string(message.text);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DiscussRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiscussRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.text = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DiscussRequest {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
      text: isSet(object.text) ? globalThis.String(object.text) : "",
    };
  },

  toJSON(message: DiscussRequest): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DiscussRequest>, I>>(base?: I): DiscussRequest {
    return DiscussRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DiscussRequest>, I>>(object: I): DiscussRequest {
    const message = createBaseDiscussRequest();
    message.taskId = object.taskId ?? "";
    message.text = object.text ?? "";
    return message;
  },
};

function createBaseDiscussResponse(): DiscussResponse {
  return { status: "" };
}

export const DiscussResponse: MessageFns<DiscussResponse> = {
  encode(message: DiscussResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DiscussResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiscussResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DiscussResponse {
    return { status: isSet(object.status) ? globalThis.String(object.status) : "" };
  },

  toJSON(message: DiscussResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DiscussResponse>, I>>(base?: I): DiscussResponse {
    return DiscussResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DiscussResponse>, I>>(object: I): DiscussResponse {
    const message = createBaseDiscussResponse();
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseDeleteTaskRequest(): DeleteTaskRequest {
  return { taskId: "" };
}

export const DeleteTaskRequest: MessageFns<DeleteTaskRequest> = {
  encode(message: DeleteTaskRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteTaskRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteTaskRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteTaskRequest {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
    };
  },

  toJSON(message: DeleteTaskRequest): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteTaskRequest>, I>>(base?: I): DeleteTaskRequest {
    return DeleteTaskRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteTaskRequest>, I>>(object: I): DeleteTaskRequest {
    const message = createBaseDeleteTaskRequest();
    message.taskId = object.taskId ?? "";
    return message;
  },
};

function createBaseDeleteTaskResponse(): DeleteTaskResponse {
  return { status: "" };
}

export const DeleteTaskResponse: MessageFns<DeleteTaskResponse> = {
  encode(message: DeleteTaskResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteTaskResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteTaskResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteTaskResponse {
    return { status: isSet(object.status) ? globalThis.String(object.status) : "" };
  },

  toJSON(message: DeleteTaskResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteTaskResponse>, I>>(base?: I): DeleteTaskResponse {
    return DeleteTaskResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteTaskResponse>, I>>(object: I): DeleteTaskResponse {
    const message = createBaseDeleteTaskResponse();
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseWorktreeView(): WorktreeView {
  return {
    taskId: "",
    repo: "",
    branch: "",
    upstreamTrack: "",
    mtimeUnix: 0,
    taskStatus: undefined,
    taskError: undefined,
    prUrl: undefined,
  };
}

export const WorktreeView: MessageFns<WorktreeView> = {
  encode(message: WorktreeView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.repo !== "") {
      writer.uint32(18).string(message.repo);
    }
    if (message.branch !== "") {
      writer.uint32(26).string(message.branch);
    }
    if (message.upstreamTrack !== "") {
      writer.uint32(34).string(message.upstreamTrack);
    }
    if (message.mtimeUnix !== 0) {
      writer.uint32(40).int64(message.mtimeUnix);
    }
    if (message.taskStatus !== undefined) {
      writer.uint32(50).string(message.taskStatus);
    }
    if (message.taskError !== undefined) {
      writer.uint32(58).string(message.taskError);
    }
    if (message.prUrl !== undefined) {
      writer.uint32(66).string(message.prUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorktreeView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorktreeView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.repo = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.branch = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.upstreamTrack = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.mtimeUnix = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.taskStatus = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.taskError = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.prUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorktreeView {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
      repo: isSet(object.repo) ? globalThis.String(object.repo) : "",
      branch: isSet(object.branch) ? globalThis.String(object.branch) : "",
      upstreamTrack: isSet(object.upstreamTrack)
        ? globalThis.String(object.upstreamTrack)
        : isSet(object.upstream_track)
        ? globalThis.String(object.upstream_track)
        : "",
      mtimeUnix: isSet(object.mtimeUnix)
        ? globalThis.Number(object.mtimeUnix)
        : isSet(object.mtime_unix)
        ? globalThis.Number(object.mtime_unix)
        : 0,
      taskStatus: isSet(object.taskStatus)
        ? globalThis.String(object.taskStatus)
        : isSet(object.task_status)
        ? globalThis.String(object.task_status)
        : undefined,
      taskError: isSet(object.taskError)
        ? globalThis.String(object.taskError)
        : isSet(object.task_error)
        ? globalThis.String(object.task_error)
        : undefined,
      prUrl: isSet(object.prUrl)
        ? globalThis.String(object.prUrl)
        : isSet(object.pr_url)
        ? globalThis.String(object.pr_url)
        : undefined,
    };
  },

  toJSON(message: WorktreeView): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.repo !== "") {
      obj.repo = message.repo;
    }
    if (message.branch !== "") {
      obj.branch = message.branch;
    }
    if (message.upstreamTrack !== "") {
      obj.upstreamTrack = message.upstreamTrack;
    }
    if (message.mtimeUnix !== 0) {
      obj.mtimeUnix = Math.round(message.mtimeUnix);
    }
    if (message.taskStatus !== undefined) {
      obj.taskStatus = message.taskStatus;
    }
    if (message.taskError !== undefined) {
      obj.taskError = message.taskError;
    }
    if (message.prUrl !== undefined) {
      obj.prUrl = message.prUrl;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorktreeView>, I>>(base?: I): WorktreeView {
    return WorktreeView.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorktreeView>, I>>(object: I): WorktreeView {
    const message = createBaseWorktreeView();
    message.taskId = object.taskId ?? "";
    message.repo = object.repo ?? "";
    message.branch = object.branch ?? "";
    message.upstreamTrack = object.upstreamTrack ?? "";
    message.mtimeUnix = object.mtimeUnix ?? 0;
    message.taskStatus = object.taskStatus ?? undefined;
    message.taskError = object.taskError ?? undefined;
    message.prUrl = object.prUrl ?? undefined;
    return message;
  },
};

function createBaseListWorktreesViewResponse(): ListWorktreesViewResponse {
  return { worktrees: [] };
}

export const ListWorktreesViewResponse: MessageFns<ListWorktreesViewResponse> = {
  encode(message: ListWorktreesViewResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.worktrees) {
      WorktreeView.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListWorktreesViewResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListWorktreesViewResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.worktrees.push(WorktreeView.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListWorktreesViewResponse {
    return {
      worktrees: globalThis.Array.isArray(object?.worktrees)
        ? object.worktrees.map((e: any) => WorktreeView.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListWorktreesViewResponse): unknown {
    const obj: any = {};
    if (message.worktrees?.length) {
      obj.worktrees = message.worktrees.map((e) => WorktreeView.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListWorktreesViewResponse>, I>>(base?: I): ListWorktreesViewResponse {
    return ListWorktreesViewResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListWorktreesViewResponse>, I>>(object: I): ListWorktreesViewResponse {
    const message = createBaseListWorktreesViewResponse();
    message.worktrees = object.worktrees?.map((e) => WorktreeView.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetJournalRequest(): GetJournalRequest {
  return { repo: "", sinceId: 0, limit: 0 };
}

export const GetJournalRequest: MessageFns<GetJournalRequest> = {
  encode(message: GetJournalRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.repo !== "") {
      writer.uint32(10).string(message.repo);
    }
    if (message.sinceId !== 0) {
      writer.uint32(16).int64(message.sinceId);
    }
    if (message.limit !== 0) {
      writer.uint32(24).int32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetJournalRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetJournalRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.repo = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sinceId = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetJournalRequest {
    return {
      repo: isSet(object.repo) ? globalThis.String(object.repo) : "",
      sinceId: isSet(object.sinceId)
        ? globalThis.Number(object.sinceId)
        : isSet(object.since_id)
        ? globalThis.Number(object.since_id)
        : 0,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
    };
  },

  toJSON(message: GetJournalRequest): unknown {
    const obj: any = {};
    if (message.repo !== "") {
      obj.repo = message.repo;
    }
    if (message.sinceId !== 0) {
      obj.sinceId = Math.round(message.sinceId);
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetJournalRequest>, I>>(base?: I): GetJournalRequest {
    return GetJournalRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetJournalRequest>, I>>(object: I): GetJournalRequest {
    const message = createBaseGetJournalRequest();
    message.repo = object.repo ?? "";
    message.sinceId = object.sinceId ?? 0;
    message.limit = object.limit ?? 0;
    return message;
  },
};

function createBaseGetJournalResponse(): GetJournalResponse {
  return { entries: [], nextId: 0 };
}

export const GetJournalResponse: MessageFns<GetJournalResponse> = {
  encode(message: GetJournalResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.entries) {
      JournalEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.nextId !== 0) {
      writer.uint32(16).int64(message.nextId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetJournalResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetJournalResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entries.push(JournalEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.nextId = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetJournalResponse {
    return {
      entries: globalThis.Array.isArray(object?.entries)
        ? object.entries.map((e: any) => JournalEntry.fromJSON(e))
        : [],
      nextId: isSet(object.nextId)
        ? globalThis.Number(object.nextId)
        : isSet(object.next_id)
        ? globalThis.Number(object.next_id)
        : 0,
    };
  },

  toJSON(message: GetJournalResponse): unknown {
    const obj: any = {};
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => JournalEntry.toJSON(e));
    }
    if (message.nextId !== 0) {
      obj.nextId = Math.round(message.nextId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetJournalResponse>, I>>(base?: I): GetJournalResponse {
    return GetJournalResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetJournalResponse>, I>>(object: I): GetJournalResponse {
    const message = createBaseGetJournalResponse();
    message.entries = object.entries?.map((e) => JournalEntry.fromPartial(e)) || [];
    message.nextId = object.nextId ?? 0;
    return message;
  },
};

function createBaseRepo(): Repo {
  return { name: "", url: "", baseBranch: "" };
}

export const Repo: MessageFns<Repo> = {
  encode(message: Repo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.url !== "") {
      writer.uint32(18).string(message.url);
    }
    if (message.baseBranch !== "") {
      writer.uint32(26).string(message.baseBranch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Repo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRepo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.baseBranch = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Repo {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      baseBranch: isSet(object.baseBranch)
        ? globalThis.String(object.baseBranch)
        : isSet(object.base_branch)
        ? globalThis.String(object.base_branch)
        : "",
    };
  },

  toJSON(message: Repo): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.baseBranch !== "") {
      obj.baseBranch = message.baseBranch;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Repo>, I>>(base?: I): Repo {
    return Repo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Repo>, I>>(object: I): Repo {
    const message = createBaseRepo();
    message.name = object.name ?? "";
    message.url = object.url ?? "";
    message.baseBranch = object.baseBranch ?? "";
    return message;
  },
};

function createBaseListReposRequest(): ListReposRequest {
  return {};
}

export const ListReposRequest: MessageFns<ListReposRequest> = {
  encode(_: ListReposRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListReposRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListReposRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListReposRequest {
    return {};
  },

  toJSON(_: ListReposRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ListReposRequest>, I>>(base?: I): ListReposRequest {
    return ListReposRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListReposRequest>, I>>(_: I): ListReposRequest {
    const message = createBaseListReposRequest();
    return message;
  },
};

function createBaseListReposResponse(): ListReposResponse {
  return { repos: [] };
}

export const ListReposResponse: MessageFns<ListReposResponse> = {
  encode(message: ListReposResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.repos) {
      Repo.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListReposResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListReposResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.repos.push(Repo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListReposResponse {
    return { repos: globalThis.Array.isArray(object?.repos) ? object.repos.map((e: any) => Repo.fromJSON(e)) : [] };
  },

  toJSON(message: ListReposResponse): unknown {
    const obj: any = {};
    if (message.repos?.length) {
      obj.repos = message.repos.map((e) => Repo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListReposResponse>, I>>(base?: I): ListReposResponse {
    return ListReposResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListReposResponse>, I>>(object: I): ListReposResponse {
    const message = createBaseListReposResponse();
    message.repos = object.repos?.map((e) => Repo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCreateRepoRequest(): CreateRepoRequest {
  return { name: "", url: "", baseBranch: "" };
}

export const CreateRepoRequest: MessageFns<CreateRepoRequest> = {
  encode(message: CreateRepoRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.url !== "") {
      writer.uint32(18).string(message.url);
    }
    if (message.baseBranch !== "") {
      writer.uint32(26).string(message.baseBranch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateRepoRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateRepoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.baseBranch = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateRepoRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      baseBranch: isSet(object.baseBranch)
        ? globalThis.String(object.baseBranch)
        : isSet(object.base_branch)
        ? globalThis.String(object.base_branch)
        : "",
    };
  },

  toJSON(message: CreateRepoRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.baseBranch !== "") {
      obj.baseBranch = message.baseBranch;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateRepoRequest>, I>>(base?: I): CreateRepoRequest {
    return CreateRepoRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateRepoRequest>, I>>(object: I): CreateRepoRequest {
    const message = createBaseCreateRepoRequest();
    message.name = object.name ?? "";
    message.url = object.url ?? "";
    message.baseBranch = object.baseBranch ?? "";
    return message;
  },
};

function createBaseCreateRepoResponse(): CreateRepoResponse {
  return { repo: undefined };
}

export const CreateRepoResponse: MessageFns<CreateRepoResponse> = {
  encode(message: CreateRepoResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.repo !== undefined) {
      Repo.encode(message.repo, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateRepoResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateRepoResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.repo = Repo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateRepoResponse {
    return { repo: isSet(object.repo) ? Repo.fromJSON(object.repo) : undefined };
  },

  toJSON(message: CreateRepoResponse): unknown {
    const obj: any = {};
    if (message.repo !== undefined) {
      obj.repo = Repo.toJSON(message.repo);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateRepoResponse>, I>>(base?: I): CreateRepoResponse {
    return CreateRepoResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateRepoResponse>, I>>(object: I): CreateRepoResponse {
    const message = createBaseCreateRepoResponse();
    message.repo = (object.repo !== undefined && object.repo !== null) ? Repo.fromPartial(object.repo) : undefined;
    return message;
  },
};

function createBaseUpdateRepoRequest(): UpdateRepoRequest {
  return { name: "", url: "", baseBranch: "" };
}

export const UpdateRepoRequest: MessageFns<UpdateRepoRequest> = {
  encode(message: UpdateRepoRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.url !== "") {
      writer.uint32(18).string(message.url);
    }
    if (message.baseBranch !== "") {
      writer.uint32(26).string(message.baseBranch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateRepoRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateRepoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.baseBranch = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateRepoRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      baseBranch: isSet(object.baseBranch)
        ? globalThis.String(object.baseBranch)
        : isSet(object.base_branch)
        ? globalThis.String(object.base_branch)
        : "",
    };
  },

  toJSON(message: UpdateRepoRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.baseBranch !== "") {
      obj.baseBranch = message.baseBranch;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateRepoRequest>, I>>(base?: I): UpdateRepoRequest {
    return UpdateRepoRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateRepoRequest>, I>>(object: I): UpdateRepoRequest {
    const message = createBaseUpdateRepoRequest();
    message.name = object.name ?? "";
    message.url = object.url ?? "";
    message.baseBranch = object.baseBranch ?? "";
    return message;
  },
};

function createBaseUpdateRepoResponse(): UpdateRepoResponse {
  return { repo: undefined };
}

export const UpdateRepoResponse: MessageFns<UpdateRepoResponse> = {
  encode(message: UpdateRepoResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.repo !== undefined) {
      Repo.encode(message.repo, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateRepoResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateRepoResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.repo = Repo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateRepoResponse {
    return { repo: isSet(object.repo) ? Repo.fromJSON(object.repo) : undefined };
  },

  toJSON(message: UpdateRepoResponse): unknown {
    const obj: any = {};
    if (message.repo !== undefined) {
      obj.repo = Repo.toJSON(message.repo);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateRepoResponse>, I>>(base?: I): UpdateRepoResponse {
    return UpdateRepoResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateRepoResponse>, I>>(object: I): UpdateRepoResponse {
    const message = createBaseUpdateRepoResponse();
    message.repo = (object.repo !== undefined && object.repo !== null) ? Repo.fromPartial(object.repo) : undefined;
    return message;
  },
};

function createBaseDeleteRepoRequest(): DeleteRepoRequest {
  return { name: "" };
}

export const DeleteRepoRequest: MessageFns<DeleteRepoRequest> = {
  encode(message: DeleteRepoRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteRepoRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteRepoRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteRepoRequest {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: DeleteRepoRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteRepoRequest>, I>>(base?: I): DeleteRepoRequest {
    return DeleteRepoRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteRepoRequest>, I>>(object: I): DeleteRepoRequest {
    const message = createBaseDeleteRepoRequest();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseDeleteRepoResponse(): DeleteRepoResponse {
  return { status: "" };
}

export const DeleteRepoResponse: MessageFns<DeleteRepoResponse> = {
  encode(message: DeleteRepoResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteRepoResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteRepoResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteRepoResponse {
    return { status: isSet(object.status) ? globalThis.String(object.status) : "" };
  },

  toJSON(message: DeleteRepoResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteRepoResponse>, I>>(base?: I): DeleteRepoResponse {
    return DeleteRepoResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteRepoResponse>, I>>(object: I): DeleteRepoResponse {
    const message = createBaseDeleteRepoResponse();
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseRepoProfile(): RepoProfile {
  return { repoName: "", name: "", startCmd: "", toolKeys: [], serviceIngredients: [] };
}

export const RepoProfile: MessageFns<RepoProfile> = {
  encode(message: RepoProfile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.repoName !== "") {
      writer.uint32(10).string(message.repoName);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.startCmd !== "") {
      writer.uint32(26).string(message.startCmd);
    }
    for (const v of message.toolKeys) {
      writer.uint32(34).string(v!);
    }
    for (const v of message.serviceIngredients) {
      ServiceIngredient.encode(v!, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RepoProfile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRepoProfile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.repoName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.startCmd = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.toolKeys.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.serviceIngredients.push(ServiceIngredient.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RepoProfile {
    return {
      repoName: isSet(object.repoName)
        ? globalThis.String(object.repoName)
        : isSet(object.repo_name)
        ? globalThis.String(object.repo_name)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      startCmd: isSet(object.startCmd)
        ? globalThis.String(object.startCmd)
        : isSet(object.start_cmd)
        ? globalThis.String(object.start_cmd)
        : "",
      toolKeys: globalThis.Array.isArray(object?.toolKeys)
        ? object.toolKeys.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.tool_keys)
        ? object.tool_keys.map((e: any) => globalThis.String(e))
        : [],
      serviceIngredients: globalThis.Array.isArray(object?.serviceIngredients)
        ? object.serviceIngredients.map((e: any) => ServiceIngredient.fromJSON(e))
        : globalThis.Array.isArray(object?.service_ingredients)
        ? object.service_ingredients.map((e: any) => ServiceIngredient.fromJSON(e))
        : [],
    };
  },

  toJSON(message: RepoProfile): unknown {
    const obj: any = {};
    if (message.repoName !== "") {
      obj.repoName = message.repoName;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.startCmd !== "") {
      obj.startCmd = message.startCmd;
    }
    if (message.toolKeys?.length) {
      obj.toolKeys = message.toolKeys;
    }
    if (message.serviceIngredients?.length) {
      obj.serviceIngredients = message.serviceIngredients.map((e) => ServiceIngredient.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RepoProfile>, I>>(base?: I): RepoProfile {
    return RepoProfile.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RepoProfile>, I>>(object: I): RepoProfile {
    const message = createBaseRepoProfile();
    message.repoName = object.repoName ?? "";
    message.name = object.name ?? "";
    message.startCmd = object.startCmd ?? "";
    message.toolKeys = object.toolKeys?.map((e) => e) || [];
    message.serviceIngredients = object.serviceIngredients?.map((e) => ServiceIngredient.fromPartial(e)) || [];
    return message;
  },
};

function createBaseListRepoProfilesRequest(): ListRepoProfilesRequest {
  return { repoName: "" };
}

export const ListRepoProfilesRequest: MessageFns<ListRepoProfilesRequest> = {
  encode(message: ListRepoProfilesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.repoName !== "") {
      writer.uint32(10).string(message.repoName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListRepoProfilesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListRepoProfilesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.repoName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListRepoProfilesRequest {
    return {
      repoName: isSet(object.repoName)
        ? globalThis.String(object.repoName)
        : isSet(object.repo_name)
        ? globalThis.String(object.repo_name)
        : "",
    };
  },

  toJSON(message: ListRepoProfilesRequest): unknown {
    const obj: any = {};
    if (message.repoName !== "") {
      obj.repoName = message.repoName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListRepoProfilesRequest>, I>>(base?: I): ListRepoProfilesRequest {
    return ListRepoProfilesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListRepoProfilesRequest>, I>>(object: I): ListRepoProfilesRequest {
    const message = createBaseListRepoProfilesRequest();
    message.repoName = object.repoName ?? "";
    return message;
  },
};

function createBaseListRepoProfilesResponse(): ListRepoProfilesResponse {
  return { profiles: [] };
}

export const ListRepoProfilesResponse: MessageFns<ListRepoProfilesResponse> = {
  encode(message: ListRepoProfilesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.profiles) {
      RepoProfile.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListRepoProfilesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListRepoProfilesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.profiles.push(RepoProfile.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListRepoProfilesResponse {
    return {
      profiles: globalThis.Array.isArray(object?.profiles)
        ? object.profiles.map((e: any) => RepoProfile.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListRepoProfilesResponse): unknown {
    const obj: any = {};
    if (message.profiles?.length) {
      obj.profiles = message.profiles.map((e) => RepoProfile.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListRepoProfilesResponse>, I>>(base?: I): ListRepoProfilesResponse {
    return ListRepoProfilesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListRepoProfilesResponse>, I>>(object: I): ListRepoProfilesResponse {
    const message = createBaseListRepoProfilesResponse();
    message.profiles = object.profiles?.map((e) => RepoProfile.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCreateRepoProfileRequest(): CreateRepoProfileRequest {
  return { repoName: "", name: "", startCmd: "", toolKeys: [], serviceIngredients: [] };
}

export const CreateRepoProfileRequest: MessageFns<CreateRepoProfileRequest> = {
  encode(message: CreateRepoProfileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.repoName !== "") {
      writer.uint32(10).string(message.repoName);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.startCmd !== "") {
      writer.uint32(26).string(message.startCmd);
    }
    for (const v of message.toolKeys) {
      writer.uint32(34).string(v!);
    }
    for (const v of message.serviceIngredients) {
      ServiceIngredient.encode(v!, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateRepoProfileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateRepoProfileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.repoName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.startCmd = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.toolKeys.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.serviceIngredients.push(ServiceIngredient.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateRepoProfileRequest {
    return {
      repoName: isSet(object.repoName)
        ? globalThis.String(object.repoName)
        : isSet(object.repo_name)
        ? globalThis.String(object.repo_name)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      startCmd: isSet(object.startCmd)
        ? globalThis.String(object.startCmd)
        : isSet(object.start_cmd)
        ? globalThis.String(object.start_cmd)
        : "",
      toolKeys: globalThis.Array.isArray(object?.toolKeys)
        ? object.toolKeys.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.tool_keys)
        ? object.tool_keys.map((e: any) => globalThis.String(e))
        : [],
      serviceIngredients: globalThis.Array.isArray(object?.serviceIngredients)
        ? object.serviceIngredients.map((e: any) => ServiceIngredient.fromJSON(e))
        : globalThis.Array.isArray(object?.service_ingredients)
        ? object.service_ingredients.map((e: any) => ServiceIngredient.fromJSON(e))
        : [],
    };
  },

  toJSON(message: CreateRepoProfileRequest): unknown {
    const obj: any = {};
    if (message.repoName !== "") {
      obj.repoName = message.repoName;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.startCmd !== "") {
      obj.startCmd = message.startCmd;
    }
    if (message.toolKeys?.length) {
      obj.toolKeys = message.toolKeys;
    }
    if (message.serviceIngredients?.length) {
      obj.serviceIngredients = message.serviceIngredients.map((e) => ServiceIngredient.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateRepoProfileRequest>, I>>(base?: I): CreateRepoProfileRequest {
    return CreateRepoProfileRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateRepoProfileRequest>, I>>(object: I): CreateRepoProfileRequest {
    const message = createBaseCreateRepoProfileRequest();
    message.repoName = object.repoName ?? "";
    message.name = object.name ?? "";
    message.startCmd = object.startCmd ?? "";
    message.toolKeys = object.toolKeys?.map((e) => e) || [];
    message.serviceIngredients = object.serviceIngredients?.map((e) => ServiceIngredient.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCreateRepoProfileResponse(): CreateRepoProfileResponse {
  return { profile: undefined };
}

export const CreateRepoProfileResponse: MessageFns<CreateRepoProfileResponse> = {
  encode(message: CreateRepoProfileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.profile !== undefined) {
      RepoProfile.encode(message.profile, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateRepoProfileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateRepoProfileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.profile = RepoProfile.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateRepoProfileResponse {
    return { profile: isSet(object.profile) ? RepoProfile.fromJSON(object.profile) : undefined };
  },

  toJSON(message: CreateRepoProfileResponse): unknown {
    const obj: any = {};
    if (message.profile !== undefined) {
      obj.profile = RepoProfile.toJSON(message.profile);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateRepoProfileResponse>, I>>(base?: I): CreateRepoProfileResponse {
    return CreateRepoProfileResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateRepoProfileResponse>, I>>(object: I): CreateRepoProfileResponse {
    const message = createBaseCreateRepoProfileResponse();
    message.profile = (object.profile !== undefined && object.profile !== null)
      ? RepoProfile.fromPartial(object.profile)
      : undefined;
    return message;
  },
};

function createBaseUpdateRepoProfileRequest(): UpdateRepoProfileRequest {
  return { repoName: "", name: "", startCmd: "", toolKeys: [], serviceIngredients: [] };
}

export const UpdateRepoProfileRequest: MessageFns<UpdateRepoProfileRequest> = {
  encode(message: UpdateRepoProfileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.repoName !== "") {
      writer.uint32(10).string(message.repoName);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.startCmd !== "") {
      writer.uint32(26).string(message.startCmd);
    }
    for (const v of message.toolKeys) {
      writer.uint32(34).string(v!);
    }
    for (const v of message.serviceIngredients) {
      ServiceIngredient.encode(v!, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateRepoProfileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateRepoProfileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.repoName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.startCmd = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.toolKeys.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.serviceIngredients.push(ServiceIngredient.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateRepoProfileRequest {
    return {
      repoName: isSet(object.repoName)
        ? globalThis.String(object.repoName)
        : isSet(object.repo_name)
        ? globalThis.String(object.repo_name)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      startCmd: isSet(object.startCmd)
        ? globalThis.String(object.startCmd)
        : isSet(object.start_cmd)
        ? globalThis.String(object.start_cmd)
        : "",
      toolKeys: globalThis.Array.isArray(object?.toolKeys)
        ? object.toolKeys.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.tool_keys)
        ? object.tool_keys.map((e: any) => globalThis.String(e))
        : [],
      serviceIngredients: globalThis.Array.isArray(object?.serviceIngredients)
        ? object.serviceIngredients.map((e: any) => ServiceIngredient.fromJSON(e))
        : globalThis.Array.isArray(object?.service_ingredients)
        ? object.service_ingredients.map((e: any) => ServiceIngredient.fromJSON(e))
        : [],
    };
  },

  toJSON(message: UpdateRepoProfileRequest): unknown {
    const obj: any = {};
    if (message.repoName !== "") {
      obj.repoName = message.repoName;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.startCmd !== "") {
      obj.startCmd = message.startCmd;
    }
    if (message.toolKeys?.length) {
      obj.toolKeys = message.toolKeys;
    }
    if (message.serviceIngredients?.length) {
      obj.serviceIngredients = message.serviceIngredients.map((e) => ServiceIngredient.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateRepoProfileRequest>, I>>(base?: I): UpdateRepoProfileRequest {
    return UpdateRepoProfileRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateRepoProfileRequest>, I>>(object: I): UpdateRepoProfileRequest {
    const message = createBaseUpdateRepoProfileRequest();
    message.repoName = object.repoName ?? "";
    message.name = object.name ?? "";
    message.startCmd = object.startCmd ?? "";
    message.toolKeys = object.toolKeys?.map((e) => e) || [];
    message.serviceIngredients = object.serviceIngredients?.map((e) => ServiceIngredient.fromPartial(e)) || [];
    return message;
  },
};

function createBaseUpdateRepoProfileResponse(): UpdateRepoProfileResponse {
  return { profile: undefined };
}

export const UpdateRepoProfileResponse: MessageFns<UpdateRepoProfileResponse> = {
  encode(message: UpdateRepoProfileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.profile !== undefined) {
      RepoProfile.encode(message.profile, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateRepoProfileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateRepoProfileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.profile = RepoProfile.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateRepoProfileResponse {
    return { profile: isSet(object.profile) ? RepoProfile.fromJSON(object.profile) : undefined };
  },

  toJSON(message: UpdateRepoProfileResponse): unknown {
    const obj: any = {};
    if (message.profile !== undefined) {
      obj.profile = RepoProfile.toJSON(message.profile);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateRepoProfileResponse>, I>>(base?: I): UpdateRepoProfileResponse {
    return UpdateRepoProfileResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateRepoProfileResponse>, I>>(object: I): UpdateRepoProfileResponse {
    const message = createBaseUpdateRepoProfileResponse();
    message.profile = (object.profile !== undefined && object.profile !== null)
      ? RepoProfile.fromPartial(object.profile)
      : undefined;
    return message;
  },
};

function createBaseDeleteRepoProfileRequest(): DeleteRepoProfileRequest {
  return { repoName: "", name: "" };
}

export const DeleteRepoProfileRequest: MessageFns<DeleteRepoProfileRequest> = {
  encode(message: DeleteRepoProfileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.repoName !== "") {
      writer.uint32(10).string(message.repoName);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteRepoProfileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteRepoProfileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.repoName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteRepoProfileRequest {
    return {
      repoName: isSet(object.repoName)
        ? globalThis.String(object.repoName)
        : isSet(object.repo_name)
        ? globalThis.String(object.repo_name)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
    };
  },

  toJSON(message: DeleteRepoProfileRequest): unknown {
    const obj: any = {};
    if (message.repoName !== "") {
      obj.repoName = message.repoName;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteRepoProfileRequest>, I>>(base?: I): DeleteRepoProfileRequest {
    return DeleteRepoProfileRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteRepoProfileRequest>, I>>(object: I): DeleteRepoProfileRequest {
    const message = createBaseDeleteRepoProfileRequest();
    message.repoName = object.repoName ?? "";
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseDeleteRepoProfileResponse(): DeleteRepoProfileResponse {
  return { status: "" };
}

export const DeleteRepoProfileResponse: MessageFns<DeleteRepoProfileResponse> = {
  encode(message: DeleteRepoProfileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteRepoProfileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteRepoProfileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteRepoProfileResponse {
    return { status: isSet(object.status) ? globalThis.String(object.status) : "" };
  },

  toJSON(message: DeleteRepoProfileResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteRepoProfileResponse>, I>>(base?: I): DeleteRepoProfileResponse {
    return DeleteRepoProfileResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteRepoProfileResponse>, I>>(object: I): DeleteRepoProfileResponse {
    const message = createBaseDeleteRepoProfileResponse();
    message.status = object.status ?? "";
    return message;
  },
};

function createBasePromptSnippet(): PromptSnippet {
  return { id: "", name: "", text: "", suggestedPermissionMode: undefined };
}

export const PromptSnippet: MessageFns<PromptSnippet> = {
  encode(message: PromptSnippet, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.text !== "") {
      writer.uint32(26).string(message.text);
    }
    if (message.suggestedPermissionMode !== undefined) {
      writer.uint32(34).string(message.suggestedPermissionMode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PromptSnippet {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePromptSnippet();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.suggestedPermissionMode = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PromptSnippet {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      suggestedPermissionMode: isSet(object.suggestedPermissionMode)
        ? globalThis.String(object.suggestedPermissionMode)
        : isSet(object.suggested_permission_mode)
        ? globalThis.String(object.suggested_permission_mode)
        : undefined,
    };
  },

  toJSON(message: PromptSnippet): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.suggestedPermissionMode !== undefined) {
      obj.suggestedPermissionMode = message.suggestedPermissionMode;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PromptSnippet>, I>>(base?: I): PromptSnippet {
    return PromptSnippet.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PromptSnippet>, I>>(object: I): PromptSnippet {
    const message = createBasePromptSnippet();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.text = object.text ?? "";
    message.suggestedPermissionMode = object.suggestedPermissionMode ?? undefined;
    return message;
  },
};

function createBaseListPromptSnippetsRequest(): ListPromptSnippetsRequest {
  return {};
}

export const ListPromptSnippetsRequest: MessageFns<ListPromptSnippetsRequest> = {
  encode(_: ListPromptSnippetsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListPromptSnippetsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListPromptSnippetsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListPromptSnippetsRequest {
    return {};
  },

  toJSON(_: ListPromptSnippetsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ListPromptSnippetsRequest>, I>>(base?: I): ListPromptSnippetsRequest {
    return ListPromptSnippetsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListPromptSnippetsRequest>, I>>(_: I): ListPromptSnippetsRequest {
    const message = createBaseListPromptSnippetsRequest();
    return message;
  },
};

function createBaseListPromptSnippetsResponse(): ListPromptSnippetsResponse {
  return { snippets: [] };
}

export const ListPromptSnippetsResponse: MessageFns<ListPromptSnippetsResponse> = {
  encode(message: ListPromptSnippetsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.snippets) {
      PromptSnippet.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListPromptSnippetsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListPromptSnippetsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.snippets.push(PromptSnippet.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListPromptSnippetsResponse {
    return {
      snippets: globalThis.Array.isArray(object?.snippets)
        ? object.snippets.map((e: any) => PromptSnippet.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListPromptSnippetsResponse): unknown {
    const obj: any = {};
    if (message.snippets?.length) {
      obj.snippets = message.snippets.map((e) => PromptSnippet.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListPromptSnippetsResponse>, I>>(base?: I): ListPromptSnippetsResponse {
    return ListPromptSnippetsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListPromptSnippetsResponse>, I>>(object: I): ListPromptSnippetsResponse {
    const message = createBaseListPromptSnippetsResponse();
    message.snippets = object.snippets?.map((e) => PromptSnippet.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCreatePromptSnippetRequest(): CreatePromptSnippetRequest {
  return { name: "", text: "" };
}

export const CreatePromptSnippetRequest: MessageFns<CreatePromptSnippetRequest> = {
  encode(message: CreatePromptSnippetRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.text !== "") {
      writer.uint32(18).string(message.text);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreatePromptSnippetRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreatePromptSnippetRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.text = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreatePromptSnippetRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      text: isSet(object.text) ? globalThis.String(object.text) : "",
    };
  },

  toJSON(message: CreatePromptSnippetRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreatePromptSnippetRequest>, I>>(base?: I): CreatePromptSnippetRequest {
    return CreatePromptSnippetRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreatePromptSnippetRequest>, I>>(object: I): CreatePromptSnippetRequest {
    const message = createBaseCreatePromptSnippetRequest();
    message.name = object.name ?? "";
    message.text = object.text ?? "";
    return message;
  },
};

function createBaseCreatePromptSnippetResponse(): CreatePromptSnippetResponse {
  return { snippet: undefined };
}

export const CreatePromptSnippetResponse: MessageFns<CreatePromptSnippetResponse> = {
  encode(message: CreatePromptSnippetResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.snippet !== undefined) {
      PromptSnippet.encode(message.snippet, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreatePromptSnippetResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreatePromptSnippetResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.snippet = PromptSnippet.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreatePromptSnippetResponse {
    return { snippet: isSet(object.snippet) ? PromptSnippet.fromJSON(object.snippet) : undefined };
  },

  toJSON(message: CreatePromptSnippetResponse): unknown {
    const obj: any = {};
    if (message.snippet !== undefined) {
      obj.snippet = PromptSnippet.toJSON(message.snippet);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreatePromptSnippetResponse>, I>>(base?: I): CreatePromptSnippetResponse {
    return CreatePromptSnippetResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreatePromptSnippetResponse>, I>>(object: I): CreatePromptSnippetResponse {
    const message = createBaseCreatePromptSnippetResponse();
    message.snippet = (object.snippet !== undefined && object.snippet !== null)
      ? PromptSnippet.fromPartial(object.snippet)
      : undefined;
    return message;
  },
};

function createBaseUpdatePromptSnippetRequest(): UpdatePromptSnippetRequest {
  return { id: "", name: "", text: "" };
}

export const UpdatePromptSnippetRequest: MessageFns<UpdatePromptSnippetRequest> = {
  encode(message: UpdatePromptSnippetRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.text !== "") {
      writer.uint32(26).string(message.text);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdatePromptSnippetRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdatePromptSnippetRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.text = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdatePromptSnippetRequest {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      text: isSet(object.text) ? globalThis.String(object.text) : "",
    };
  },

  toJSON(message: UpdatePromptSnippetRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdatePromptSnippetRequest>, I>>(base?: I): UpdatePromptSnippetRequest {
    return UpdatePromptSnippetRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdatePromptSnippetRequest>, I>>(object: I): UpdatePromptSnippetRequest {
    const message = createBaseUpdatePromptSnippetRequest();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.text = object.text ?? "";
    return message;
  },
};

function createBaseUpdatePromptSnippetResponse(): UpdatePromptSnippetResponse {
  return { snippet: undefined };
}

export const UpdatePromptSnippetResponse: MessageFns<UpdatePromptSnippetResponse> = {
  encode(message: UpdatePromptSnippetResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.snippet !== undefined) {
      PromptSnippet.encode(message.snippet, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdatePromptSnippetResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdatePromptSnippetResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.snippet = PromptSnippet.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdatePromptSnippetResponse {
    return { snippet: isSet(object.snippet) ? PromptSnippet.fromJSON(object.snippet) : undefined };
  },

  toJSON(message: UpdatePromptSnippetResponse): unknown {
    const obj: any = {};
    if (message.snippet !== undefined) {
      obj.snippet = PromptSnippet.toJSON(message.snippet);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdatePromptSnippetResponse>, I>>(base?: I): UpdatePromptSnippetResponse {
    return UpdatePromptSnippetResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdatePromptSnippetResponse>, I>>(object: I): UpdatePromptSnippetResponse {
    const message = createBaseUpdatePromptSnippetResponse();
    message.snippet = (object.snippet !== undefined && object.snippet !== null)
      ? PromptSnippet.fromPartial(object.snippet)
      : undefined;
    return message;
  },
};

function createBaseDeletePromptSnippetRequest(): DeletePromptSnippetRequest {
  return { id: "" };
}

export const DeletePromptSnippetRequest: MessageFns<DeletePromptSnippetRequest> = {
  encode(message: DeletePromptSnippetRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeletePromptSnippetRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeletePromptSnippetRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeletePromptSnippetRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: DeletePromptSnippetRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeletePromptSnippetRequest>, I>>(base?: I): DeletePromptSnippetRequest {
    return DeletePromptSnippetRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeletePromptSnippetRequest>, I>>(object: I): DeletePromptSnippetRequest {
    const message = createBaseDeletePromptSnippetRequest();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseDeletePromptSnippetResponse(): DeletePromptSnippetResponse {
  return { status: "" };
}

export const DeletePromptSnippetResponse: MessageFns<DeletePromptSnippetResponse> = {
  encode(message: DeletePromptSnippetResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeletePromptSnippetResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeletePromptSnippetResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeletePromptSnippetResponse {
    return { status: isSet(object.status) ? globalThis.String(object.status) : "" };
  },

  toJSON(message: DeletePromptSnippetResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeletePromptSnippetResponse>, I>>(base?: I): DeletePromptSnippetResponse {
    return DeletePromptSnippetResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeletePromptSnippetResponse>, I>>(object: I): DeletePromptSnippetResponse {
    const message = createBaseDeletePromptSnippetResponse();
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseScheduledAudit(): ScheduledAudit {
  return {
    id: "",
    name: "",
    prompt: "",
    intervalSeconds: 0,
    enabled: false,
    nextRunAt: "",
    lastRunAt: "",
    lastStatus: "",
  };
}

export const ScheduledAudit: MessageFns<ScheduledAudit> = {
  encode(message: ScheduledAudit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.prompt !== "") {
      writer.uint32(26).string(message.prompt);
    }
    if (message.intervalSeconds !== 0) {
      writer.uint32(32).int32(message.intervalSeconds);
    }
    if (message.enabled !== false) {
      writer.uint32(40).bool(message.enabled);
    }
    if (message.nextRunAt !== "") {
      writer.uint32(50).string(message.nextRunAt);
    }
    if (message.lastRunAt !== "") {
      writer.uint32(58).string(message.lastRunAt);
    }
    if (message.lastStatus !== "") {
      writer.uint32(66).string(message.lastStatus);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScheduledAudit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScheduledAudit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.prompt = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.intervalSeconds = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.nextRunAt = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.lastRunAt = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.lastStatus = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScheduledAudit {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      prompt: isSet(object.prompt) ? globalThis.String(object.prompt) : "",
      intervalSeconds: isSet(object.intervalSeconds)
        ? globalThis.Number(object.intervalSeconds)
        : isSet(object.interval_seconds)
        ? globalThis.Number(object.interval_seconds)
        : 0,
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      nextRunAt: isSet(object.nextRunAt)
        ? globalThis.String(object.nextRunAt)
        : isSet(object.next_run_at)
        ? globalThis.String(object.next_run_at)
        : "",
      lastRunAt: isSet(object.lastRunAt)
        ? globalThis.String(object.lastRunAt)
        : isSet(object.last_run_at)
        ? globalThis.String(object.last_run_at)
        : "",
      lastStatus: isSet(object.lastStatus)
        ? globalThis.String(object.lastStatus)
        : isSet(object.last_status)
        ? globalThis.String(object.last_status)
        : "",
    };
  },

  toJSON(message: ScheduledAudit): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.prompt !== "") {
      obj.prompt = message.prompt;
    }
    if (message.intervalSeconds !== 0) {
      obj.intervalSeconds = Math.round(message.intervalSeconds);
    }
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.nextRunAt !== "") {
      obj.nextRunAt = message.nextRunAt;
    }
    if (message.lastRunAt !== "") {
      obj.lastRunAt = message.lastRunAt;
    }
    if (message.lastStatus !== "") {
      obj.lastStatus = message.lastStatus;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ScheduledAudit>, I>>(base?: I): ScheduledAudit {
    return ScheduledAudit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ScheduledAudit>, I>>(object: I): ScheduledAudit {
    const message = createBaseScheduledAudit();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.prompt = object.prompt ?? "";
    message.intervalSeconds = object.intervalSeconds ?? 0;
    message.enabled = object.enabled ?? false;
    message.nextRunAt = object.nextRunAt ?? "";
    message.lastRunAt = object.lastRunAt ?? "";
    message.lastStatus = object.lastStatus ?? "";
    return message;
  },
};

function createBaseListScheduledAuditsRequest(): ListScheduledAuditsRequest {
  return {};
}

export const ListScheduledAuditsRequest: MessageFns<ListScheduledAuditsRequest> = {
  encode(_: ListScheduledAuditsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListScheduledAuditsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListScheduledAuditsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListScheduledAuditsRequest {
    return {};
  },

  toJSON(_: ListScheduledAuditsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ListScheduledAuditsRequest>, I>>(base?: I): ListScheduledAuditsRequest {
    return ListScheduledAuditsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListScheduledAuditsRequest>, I>>(_: I): ListScheduledAuditsRequest {
    const message = createBaseListScheduledAuditsRequest();
    return message;
  },
};

function createBaseListScheduledAuditsResponse(): ListScheduledAuditsResponse {
  return { audits: [] };
}

export const ListScheduledAuditsResponse: MessageFns<ListScheduledAuditsResponse> = {
  encode(message: ListScheduledAuditsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.audits) {
      ScheduledAudit.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListScheduledAuditsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListScheduledAuditsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.audits.push(ScheduledAudit.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListScheduledAuditsResponse {
    return {
      audits: globalThis.Array.isArray(object?.audits) ? object.audits.map((e: any) => ScheduledAudit.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListScheduledAuditsResponse): unknown {
    const obj: any = {};
    if (message.audits?.length) {
      obj.audits = message.audits.map((e) => ScheduledAudit.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListScheduledAuditsResponse>, I>>(base?: I): ListScheduledAuditsResponse {
    return ListScheduledAuditsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListScheduledAuditsResponse>, I>>(object: I): ListScheduledAuditsResponse {
    const message = createBaseListScheduledAuditsResponse();
    message.audits = object.audits?.map((e) => ScheduledAudit.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCreateScheduledAuditRequest(): CreateScheduledAuditRequest {
  return { name: "", prompt: "", intervalSeconds: 0 };
}

export const CreateScheduledAuditRequest: MessageFns<CreateScheduledAuditRequest> = {
  encode(message: CreateScheduledAuditRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.prompt !== "") {
      writer.uint32(18).string(message.prompt);
    }
    if (message.intervalSeconds !== 0) {
      writer.uint32(24).int32(message.intervalSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateScheduledAuditRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateScheduledAuditRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.prompt = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.intervalSeconds = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateScheduledAuditRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      prompt: isSet(object.prompt) ? globalThis.String(object.prompt) : "",
      intervalSeconds: isSet(object.intervalSeconds)
        ? globalThis.Number(object.intervalSeconds)
        : isSet(object.interval_seconds)
        ? globalThis.Number(object.interval_seconds)
        : 0,
    };
  },

  toJSON(message: CreateScheduledAuditRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.prompt !== "") {
      obj.prompt = message.prompt;
    }
    if (message.intervalSeconds !== 0) {
      obj.intervalSeconds = Math.round(message.intervalSeconds);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateScheduledAuditRequest>, I>>(base?: I): CreateScheduledAuditRequest {
    return CreateScheduledAuditRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateScheduledAuditRequest>, I>>(object: I): CreateScheduledAuditRequest {
    const message = createBaseCreateScheduledAuditRequest();
    message.name = object.name ?? "";
    message.prompt = object.prompt ?? "";
    message.intervalSeconds = object.intervalSeconds ?? 0;
    return message;
  },
};

function createBaseCreateScheduledAuditResponse(): CreateScheduledAuditResponse {
  return { audit: undefined };
}

export const CreateScheduledAuditResponse: MessageFns<CreateScheduledAuditResponse> = {
  encode(message: CreateScheduledAuditResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.audit !== undefined) {
      ScheduledAudit.encode(message.audit, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateScheduledAuditResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateScheduledAuditResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.audit = ScheduledAudit.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateScheduledAuditResponse {
    return { audit: isSet(object.audit) ? ScheduledAudit.fromJSON(object.audit) : undefined };
  },

  toJSON(message: CreateScheduledAuditResponse): unknown {
    const obj: any = {};
    if (message.audit !== undefined) {
      obj.audit = ScheduledAudit.toJSON(message.audit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateScheduledAuditResponse>, I>>(base?: I): CreateScheduledAuditResponse {
    return CreateScheduledAuditResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateScheduledAuditResponse>, I>>(object: I): CreateScheduledAuditResponse {
    const message = createBaseCreateScheduledAuditResponse();
    message.audit = (object.audit !== undefined && object.audit !== null)
      ? ScheduledAudit.fromPartial(object.audit)
      : undefined;
    return message;
  },
};

function createBaseUpdateScheduledAuditRequest(): UpdateScheduledAuditRequest {
  return { id: "", name: "", prompt: "", intervalSeconds: 0, enabled: false };
}

export const UpdateScheduledAuditRequest: MessageFns<UpdateScheduledAuditRequest> = {
  encode(message: UpdateScheduledAuditRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.prompt !== "") {
      writer.uint32(26).string(message.prompt);
    }
    if (message.intervalSeconds !== 0) {
      writer.uint32(32).int32(message.intervalSeconds);
    }
    if (message.enabled !== false) {
      writer.uint32(40).bool(message.enabled);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateScheduledAuditRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateScheduledAuditRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.prompt = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.intervalSeconds = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateScheduledAuditRequest {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      prompt: isSet(object.prompt) ? globalThis.String(object.prompt) : "",
      intervalSeconds: isSet(object.intervalSeconds)
        ? globalThis.Number(object.intervalSeconds)
        : isSet(object.interval_seconds)
        ? globalThis.Number(object.interval_seconds)
        : 0,
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
    };
  },

  toJSON(message: UpdateScheduledAuditRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.prompt !== "") {
      obj.prompt = message.prompt;
    }
    if (message.intervalSeconds !== 0) {
      obj.intervalSeconds = Math.round(message.intervalSeconds);
    }
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateScheduledAuditRequest>, I>>(base?: I): UpdateScheduledAuditRequest {
    return UpdateScheduledAuditRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateScheduledAuditRequest>, I>>(object: I): UpdateScheduledAuditRequest {
    const message = createBaseUpdateScheduledAuditRequest();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.prompt = object.prompt ?? "";
    message.intervalSeconds = object.intervalSeconds ?? 0;
    message.enabled = object.enabled ?? false;
    return message;
  },
};

function createBaseUpdateScheduledAuditResponse(): UpdateScheduledAuditResponse {
  return { audit: undefined };
}

export const UpdateScheduledAuditResponse: MessageFns<UpdateScheduledAuditResponse> = {
  encode(message: UpdateScheduledAuditResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.audit !== undefined) {
      ScheduledAudit.encode(message.audit, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateScheduledAuditResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateScheduledAuditResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.audit = ScheduledAudit.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateScheduledAuditResponse {
    return { audit: isSet(object.audit) ? ScheduledAudit.fromJSON(object.audit) : undefined };
  },

  toJSON(message: UpdateScheduledAuditResponse): unknown {
    const obj: any = {};
    if (message.audit !== undefined) {
      obj.audit = ScheduledAudit.toJSON(message.audit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateScheduledAuditResponse>, I>>(base?: I): UpdateScheduledAuditResponse {
    return UpdateScheduledAuditResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateScheduledAuditResponse>, I>>(object: I): UpdateScheduledAuditResponse {
    const message = createBaseUpdateScheduledAuditResponse();
    message.audit = (object.audit !== undefined && object.audit !== null)
      ? ScheduledAudit.fromPartial(object.audit)
      : undefined;
    return message;
  },
};

function createBaseDeleteScheduledAuditRequest(): DeleteScheduledAuditRequest {
  return { id: "" };
}

export const DeleteScheduledAuditRequest: MessageFns<DeleteScheduledAuditRequest> = {
  encode(message: DeleteScheduledAuditRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteScheduledAuditRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteScheduledAuditRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteScheduledAuditRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: DeleteScheduledAuditRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteScheduledAuditRequest>, I>>(base?: I): DeleteScheduledAuditRequest {
    return DeleteScheduledAuditRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteScheduledAuditRequest>, I>>(object: I): DeleteScheduledAuditRequest {
    const message = createBaseDeleteScheduledAuditRequest();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseDeleteScheduledAuditResponse(): DeleteScheduledAuditResponse {
  return { status: "" };
}

export const DeleteScheduledAuditResponse: MessageFns<DeleteScheduledAuditResponse> = {
  encode(message: DeleteScheduledAuditResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteScheduledAuditResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteScheduledAuditResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteScheduledAuditResponse {
    return { status: isSet(object.status) ? globalThis.String(object.status) : "" };
  },

  toJSON(message: DeleteScheduledAuditResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteScheduledAuditResponse>, I>>(base?: I): DeleteScheduledAuditResponse {
    return DeleteScheduledAuditResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteScheduledAuditResponse>, I>>(object: I): DeleteScheduledAuditResponse {
    const message = createBaseDeleteScheduledAuditResponse();
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseThotEvent(): ThotEvent {
  return { id: 0, kind: "", actor: "", payload: "", replyTo: undefined, createdAt: "" };
}

export const ThotEvent: MessageFns<ThotEvent> = {
  encode(message: ThotEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int64(message.id);
    }
    if (message.kind !== "") {
      writer.uint32(18).string(message.kind);
    }
    if (message.actor !== "") {
      writer.uint32(26).string(message.actor);
    }
    if (message.payload !== "") {
      writer.uint32(34).string(message.payload);
    }
    if (message.replyTo !== undefined) {
      writer.uint32(40).int64(message.replyTo);
    }
    if (message.createdAt !== "") {
      writer.uint32(50).string(message.createdAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ThotEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseThotEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.kind = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.actor = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.payload = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.replyTo = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ThotEvent {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      kind: isSet(object.kind) ? globalThis.String(object.kind) : "",
      actor: isSet(object.actor) ? globalThis.String(object.actor) : "",
      payload: isSet(object.payload) ? globalThis.String(object.payload) : "",
      replyTo: isSet(object.replyTo)
        ? globalThis.Number(object.replyTo)
        : isSet(object.reply_to)
        ? globalThis.Number(object.reply_to)
        : undefined,
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
    };
  },

  toJSON(message: ThotEvent): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.kind !== "") {
      obj.kind = message.kind;
    }
    if (message.actor !== "") {
      obj.actor = message.actor;
    }
    if (message.payload !== "") {
      obj.payload = message.payload;
    }
    if (message.replyTo !== undefined) {
      obj.replyTo = Math.round(message.replyTo);
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ThotEvent>, I>>(base?: I): ThotEvent {
    return ThotEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ThotEvent>, I>>(object: I): ThotEvent {
    const message = createBaseThotEvent();
    message.id = object.id ?? 0;
    message.kind = object.kind ?? "";
    message.actor = object.actor ?? "";
    message.payload = object.payload ?? "";
    message.replyTo = object.replyTo ?? undefined;
    message.createdAt = object.createdAt ?? "";
    return message;
  },
};

function createBaseListThotEventsRequest(): ListThotEventsRequest {
  return { sinceId: 0, limit: 0 };
}

export const ListThotEventsRequest: MessageFns<ListThotEventsRequest> = {
  encode(message: ListThotEventsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sinceId !== 0) {
      writer.uint32(8).int64(message.sinceId);
    }
    if (message.limit !== 0) {
      writer.uint32(16).int32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListThotEventsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListThotEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.sinceId = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListThotEventsRequest {
    return {
      sinceId: isSet(object.sinceId)
        ? globalThis.Number(object.sinceId)
        : isSet(object.since_id)
        ? globalThis.Number(object.since_id)
        : 0,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
    };
  },

  toJSON(message: ListThotEventsRequest): unknown {
    const obj: any = {};
    if (message.sinceId !== 0) {
      obj.sinceId = Math.round(message.sinceId);
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListThotEventsRequest>, I>>(base?: I): ListThotEventsRequest {
    return ListThotEventsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListThotEventsRequest>, I>>(object: I): ListThotEventsRequest {
    const message = createBaseListThotEventsRequest();
    message.sinceId = object.sinceId ?? 0;
    message.limit = object.limit ?? 0;
    return message;
  },
};

function createBaseListThotEventsResponse(): ListThotEventsResponse {
  return { events: [], nextId: 0, pending: [] };
}

export const ListThotEventsResponse: MessageFns<ListThotEventsResponse> = {
  encode(message: ListThotEventsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.events) {
      ThotEvent.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.nextId !== 0) {
      writer.uint32(16).int64(message.nextId);
    }
    for (const v of message.pending) {
      ThotEvent.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListThotEventsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListThotEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.events.push(ThotEvent.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.nextId = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.pending.push(ThotEvent.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListThotEventsResponse {
    return {
      events: globalThis.Array.isArray(object?.events) ? object.events.map((e: any) => ThotEvent.fromJSON(e)) : [],
      nextId: isSet(object.nextId)
        ? globalThis.Number(object.nextId)
        : isSet(object.next_id)
        ? globalThis.Number(object.next_id)
        : 0,
      pending: globalThis.Array.isArray(object?.pending) ? object.pending.map((e: any) => ThotEvent.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListThotEventsResponse): unknown {
    const obj: any = {};
    if (message.events?.length) {
      obj.events = message.events.map((e) => ThotEvent.toJSON(e));
    }
    if (message.nextId !== 0) {
      obj.nextId = Math.round(message.nextId);
    }
    if (message.pending?.length) {
      obj.pending = message.pending.map((e) => ThotEvent.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListThotEventsResponse>, I>>(base?: I): ListThotEventsResponse {
    return ListThotEventsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListThotEventsResponse>, I>>(object: I): ListThotEventsResponse {
    const message = createBaseListThotEventsResponse();
    message.events = object.events?.map((e) => ThotEvent.fromPartial(e)) || [];
    message.nextId = object.nextId ?? 0;
    message.pending = object.pending?.map((e) => ThotEvent.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRespondToThotPermissionRequest(): RespondToThotPermissionRequest {
  return { requestId: 0, allow: false, message: "" };
}

export const RespondToThotPermissionRequest: MessageFns<RespondToThotPermissionRequest> = {
  encode(message: RespondToThotPermissionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.requestId !== 0) {
      writer.uint32(8).int64(message.requestId);
    }
    if (message.allow !== false) {
      writer.uint32(16).bool(message.allow);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RespondToThotPermissionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRespondToThotPermissionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.requestId = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.allow = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RespondToThotPermissionRequest {
    return {
      requestId: isSet(object.requestId)
        ? globalThis.Number(object.requestId)
        : isSet(object.request_id)
        ? globalThis.Number(object.request_id)
        : 0,
      allow: isSet(object.allow) ? globalThis.Boolean(object.allow) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: RespondToThotPermissionRequest): unknown {
    const obj: any = {};
    if (message.requestId !== 0) {
      obj.requestId = Math.round(message.requestId);
    }
    if (message.allow !== false) {
      obj.allow = message.allow;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RespondToThotPermissionRequest>, I>>(base?: I): RespondToThotPermissionRequest {
    return RespondToThotPermissionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RespondToThotPermissionRequest>, I>>(
    object: I,
  ): RespondToThotPermissionRequest {
    const message = createBaseRespondToThotPermissionRequest();
    message.requestId = object.requestId ?? 0;
    message.allow = object.allow ?? false;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseRespondToThotPermissionResponse(): RespondToThotPermissionResponse {
  return { status: "" };
}

export const RespondToThotPermissionResponse: MessageFns<RespondToThotPermissionResponse> = {
  encode(message: RespondToThotPermissionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RespondToThotPermissionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRespondToThotPermissionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RespondToThotPermissionResponse {
    return { status: isSet(object.status) ? globalThis.String(object.status) : "" };
  },

  toJSON(message: RespondToThotPermissionResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RespondToThotPermissionResponse>, I>>(base?: I): RespondToThotPermissionResponse {
    return RespondToThotPermissionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RespondToThotPermissionResponse>, I>>(
    object: I,
  ): RespondToThotPermissionResponse {
    const message = createBaseRespondToThotPermissionResponse();
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseDashboardServiceAskThotRequest(): DashboardServiceAskThotRequest {
  return { question: "" };
}

export const DashboardServiceAskThotRequest: MessageFns<DashboardServiceAskThotRequest> = {
  encode(message: DashboardServiceAskThotRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.question !== "") {
      writer.uint32(10).string(message.question);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DashboardServiceAskThotRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDashboardServiceAskThotRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.question = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DashboardServiceAskThotRequest {
    return { question: isSet(object.question) ? globalThis.String(object.question) : "" };
  },

  toJSON(message: DashboardServiceAskThotRequest): unknown {
    const obj: any = {};
    if (message.question !== "") {
      obj.question = message.question;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DashboardServiceAskThotRequest>, I>>(base?: I): DashboardServiceAskThotRequest {
    return DashboardServiceAskThotRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DashboardServiceAskThotRequest>, I>>(
    object: I,
  ): DashboardServiceAskThotRequest {
    const message = createBaseDashboardServiceAskThotRequest();
    message.question = object.question ?? "";
    return message;
  },
};

function createBaseDashboardServiceAskThotResponse(): DashboardServiceAskThotResponse {
  return { answer: "" };
}

export const DashboardServiceAskThotResponse: MessageFns<DashboardServiceAskThotResponse> = {
  encode(message: DashboardServiceAskThotResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.answer !== "") {
      writer.uint32(10).string(message.answer);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DashboardServiceAskThotResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDashboardServiceAskThotResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.answer = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DashboardServiceAskThotResponse {
    return { answer: isSet(object.answer) ? globalThis.String(object.answer) : "" };
  },

  toJSON(message: DashboardServiceAskThotResponse): unknown {
    const obj: any = {};
    if (message.answer !== "") {
      obj.answer = message.answer;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DashboardServiceAskThotResponse>, I>>(base?: I): DashboardServiceAskThotResponse {
    return DashboardServiceAskThotResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DashboardServiceAskThotResponse>, I>>(
    object: I,
  ): DashboardServiceAskThotResponse {
    const message = createBaseDashboardServiceAskThotResponse();
    message.answer = object.answer ?? "";
    return message;
  },
};

export type DashboardServiceService = typeof DashboardServiceService;
export const DashboardServiceService = {
  listTasks: {
    path: "/agentfleet.v1.DashboardService/ListTasks" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListTasksRequest): Buffer => Buffer.from(ListTasksRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListTasksRequest => ListTasksRequest.decode(value),
    responseSerialize: (value: ListTasksResponse): Buffer => Buffer.from(ListTasksResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListTasksResponse => ListTasksResponse.decode(value),
  },
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  getTask: {
    path: "/agentfleet.v1.DashboardService/GetTask" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetTaskRequest): Buffer => Buffer.from(GetTaskRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetTaskRequest => GetTaskRequest.decode(value),
    responseSerialize: (value: GetTaskResponse): Buffer => Buffer.from(GetTaskResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetTaskResponse => GetTaskResponse.decode(value),
  },
  createTask: {
    path: "/agentfleet.v1.DashboardService/CreateTask" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CreateTaskRequest): Buffer => Buffer.from(CreateTaskRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CreateTaskRequest => CreateTaskRequest.decode(value),
    responseSerialize: (value: CreateTaskResponse): Buffer => Buffer.from(CreateTaskResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CreateTaskResponse => CreateTaskResponse.decode(value),
  },
  /**
   * Reuses transcript.proto's ReadTranscriptSinceRequest/Response rather
   * than duplicating a byte-identical message pair.
   * buf:lint:ignore RPC_REQUEST_STANDARD_NAME
   * buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
   * buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE
   */
  getTranscript: {
    path: "/agentfleet.v1.DashboardService/GetTranscript" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ReadTranscriptSinceRequest): Buffer =>
      Buffer.from(ReadTranscriptSinceRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ReadTranscriptSinceRequest => ReadTranscriptSinceRequest.decode(value),
    responseSerialize: (value: ReadTranscriptSinceResponse): Buffer =>
      Buffer.from(ReadTranscriptSinceResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ReadTranscriptSinceResponse => ReadTranscriptSinceResponse.decode(value),
  },
  /**
   * Streams transcript.proto's TranscriptEntry directly rather than
   * wrapping it in a redundant StreamTranscriptResponse.
   * buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
   * buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE
   */
  streamTranscript: {
    path: "/agentfleet.v1.DashboardService/StreamTranscript" as const,
    requestStream: false as const,
    responseStream: true as const,
    requestSerialize: (value: StreamTranscriptRequest): Buffer =>
      Buffer.from(StreamTranscriptRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): StreamTranscriptRequest => StreamTranscriptRequest.decode(value),
    responseSerialize: (value: TranscriptEntry): Buffer => Buffer.from(TranscriptEntry.encode(value).finish()),
    responseDeserialize: (value: Buffer): TranscriptEntry => TranscriptEntry.decode(value),
  },
  getE2EStatus: {
    path: "/agentfleet.v1.DashboardService/GetE2eStatus" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetE2eStatusRequest): Buffer => Buffer.from(GetE2eStatusRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetE2eStatusRequest => GetE2eStatusRequest.decode(value),
    responseSerialize: (value: GetE2eStatusResponse): Buffer =>
      Buffer.from(GetE2eStatusResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetE2eStatusResponse => GetE2eStatusResponse.decode(value),
  },
  kill: {
    path: "/agentfleet.v1.DashboardService/Kill" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: KillRequest): Buffer => Buffer.from(KillRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): KillRequest => KillRequest.decode(value),
    responseSerialize: (value: KillResponse): Buffer => Buffer.from(KillResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): KillResponse => KillResponse.decode(value),
  },
  interrupt: {
    path: "/agentfleet.v1.DashboardService/Interrupt" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: InterruptRequest): Buffer => Buffer.from(InterruptRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): InterruptRequest => InterruptRequest.decode(value),
    responseSerialize: (value: InterruptResponse): Buffer => Buffer.from(InterruptResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): InterruptResponse => InterruptResponse.decode(value),
  },
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  setPermissionMode: {
    path: "/agentfleet.v1.DashboardService/SetPermissionMode" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: SetPermissionModeRequest): Buffer =>
      Buffer.from(SetPermissionModeRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SetPermissionModeRequest => SetPermissionModeRequest.decode(value),
    responseSerialize: (value: SetPermissionModeResponse): Buffer =>
      Buffer.from(SetPermissionModeResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SetPermissionModeResponse => SetPermissionModeResponse.decode(value),
  },
  warm: {
    path: "/agentfleet.v1.DashboardService/Warm" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: WarmRequest): Buffer => Buffer.from(WarmRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): WarmRequest => WarmRequest.decode(value),
    responseSerialize: (value: WarmResponse): Buffer => Buffer.from(WarmResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): WarmResponse => WarmResponse.decode(value),
  },
  killE2E: {
    path: "/agentfleet.v1.DashboardService/KillE2e" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: KillE2eRequest): Buffer => Buffer.from(KillE2eRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): KillE2eRequest => KillE2eRequest.decode(value),
    responseSerialize: (value: KillE2eResponse): Buffer => Buffer.from(KillE2eResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): KillE2eResponse => KillE2eResponse.decode(value),
  },
  answerQuestion: {
    path: "/agentfleet.v1.DashboardService/AnswerQuestion" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: AnswerQuestionRequest): Buffer =>
      Buffer.from(AnswerQuestionRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): AnswerQuestionRequest => AnswerQuestionRequest.decode(value),
    responseSerialize: (value: AnswerQuestionResponse): Buffer =>
      Buffer.from(AnswerQuestionResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): AnswerQuestionResponse => AnswerQuestionResponse.decode(value),
  },
  respondToPermission: {
    path: "/agentfleet.v1.DashboardService/RespondToPermission" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RespondToPermissionRequest): Buffer =>
      Buffer.from(RespondToPermissionRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RespondToPermissionRequest => RespondToPermissionRequest.decode(value),
    responseSerialize: (value: RespondToPermissionResponse): Buffer =>
      Buffer.from(RespondToPermissionResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RespondToPermissionResponse => RespondToPermissionResponse.decode(value),
  },
  discuss: {
    path: "/agentfleet.v1.DashboardService/Discuss" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DiscussRequest): Buffer => Buffer.from(DiscussRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DiscussRequest => DiscussRequest.decode(value),
    responseSerialize: (value: DiscussResponse): Buffer => Buffer.from(DiscussResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DiscussResponse => DiscussResponse.decode(value),
  },
  deleteTask: {
    path: "/agentfleet.v1.DashboardService/DeleteTask" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DeleteTaskRequest): Buffer => Buffer.from(DeleteTaskRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DeleteTaskRequest => DeleteTaskRequest.decode(value),
    responseSerialize: (value: DeleteTaskResponse): Buffer => Buffer.from(DeleteTaskResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DeleteTaskResponse => DeleteTaskResponse.decode(value),
  },
  /**
   * Reuses provisioner.proto's ListWorktreesRequest (identical shape,
   * empty) and DeleteWorktreeRequest/Response (identical shape, no
   * dashboard-specific fields needed) — same passthrough-reuse pattern
   * core.proto's ListE2eTools/CallE2eTool already established. The
   * response is enriched with a left-join against `tasks`, so it can't
   * reuse provisioner's own WorktreeInfo/ListWorktreesResponse as-is.
   * buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE
   * buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
   */
  listWorktrees: {
    path: "/agentfleet.v1.DashboardService/ListWorktrees" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListWorktreesRequest): Buffer => Buffer.from(ListWorktreesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListWorktreesRequest => ListWorktreesRequest.decode(value),
    responseSerialize: (value: ListWorktreesViewResponse): Buffer =>
      Buffer.from(ListWorktreesViewResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListWorktreesViewResponse => ListWorktreesViewResponse.decode(value),
  },
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  deleteWorktree: {
    path: "/agentfleet.v1.DashboardService/DeleteWorktree" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DeleteWorktreeRequest): Buffer =>
      Buffer.from(DeleteWorktreeRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DeleteWorktreeRequest => DeleteWorktreeRequest.decode(value),
    responseSerialize: (value: DeleteWorktreeResponse): Buffer =>
      Buffer.from(DeleteWorktreeResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DeleteWorktreeResponse => DeleteWorktreeResponse.decode(value),
  },
  getJournal: {
    path: "/agentfleet.v1.DashboardService/GetJournal" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetJournalRequest): Buffer => Buffer.from(GetJournalRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetJournalRequest => GetJournalRequest.decode(value),
    responseSerialize: (value: GetJournalResponse): Buffer => Buffer.from(GetJournalResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetJournalResponse => GetJournalResponse.decode(value),
  },
  listRepos: {
    path: "/agentfleet.v1.DashboardService/ListRepos" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListReposRequest): Buffer => Buffer.from(ListReposRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListReposRequest => ListReposRequest.decode(value),
    responseSerialize: (value: ListReposResponse): Buffer => Buffer.from(ListReposResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListReposResponse => ListReposResponse.decode(value),
  },
  createRepo: {
    path: "/agentfleet.v1.DashboardService/CreateRepo" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CreateRepoRequest): Buffer => Buffer.from(CreateRepoRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CreateRepoRequest => CreateRepoRequest.decode(value),
    responseSerialize: (value: CreateRepoResponse): Buffer => Buffer.from(CreateRepoResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CreateRepoResponse => CreateRepoResponse.decode(value),
  },
  updateRepo: {
    path: "/agentfleet.v1.DashboardService/UpdateRepo" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: UpdateRepoRequest): Buffer => Buffer.from(UpdateRepoRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): UpdateRepoRequest => UpdateRepoRequest.decode(value),
    responseSerialize: (value: UpdateRepoResponse): Buffer => Buffer.from(UpdateRepoResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): UpdateRepoResponse => UpdateRepoResponse.decode(value),
  },
  deleteRepo: {
    path: "/agentfleet.v1.DashboardService/DeleteRepo" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DeleteRepoRequest): Buffer => Buffer.from(DeleteRepoRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DeleteRepoRequest => DeleteRepoRequest.decode(value),
    responseSerialize: (value: DeleteRepoResponse): Buffer => Buffer.from(DeleteRepoResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DeleteRepoResponse => DeleteRepoResponse.decode(value),
  },
  listRepoProfiles: {
    path: "/agentfleet.v1.DashboardService/ListRepoProfiles" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListRepoProfilesRequest): Buffer =>
      Buffer.from(ListRepoProfilesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListRepoProfilesRequest => ListRepoProfilesRequest.decode(value),
    responseSerialize: (value: ListRepoProfilesResponse): Buffer =>
      Buffer.from(ListRepoProfilesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListRepoProfilesResponse => ListRepoProfilesResponse.decode(value),
  },
  createRepoProfile: {
    path: "/agentfleet.v1.DashboardService/CreateRepoProfile" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CreateRepoProfileRequest): Buffer =>
      Buffer.from(CreateRepoProfileRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CreateRepoProfileRequest => CreateRepoProfileRequest.decode(value),
    responseSerialize: (value: CreateRepoProfileResponse): Buffer =>
      Buffer.from(CreateRepoProfileResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CreateRepoProfileResponse => CreateRepoProfileResponse.decode(value),
  },
  updateRepoProfile: {
    path: "/agentfleet.v1.DashboardService/UpdateRepoProfile" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: UpdateRepoProfileRequest): Buffer =>
      Buffer.from(UpdateRepoProfileRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): UpdateRepoProfileRequest => UpdateRepoProfileRequest.decode(value),
    responseSerialize: (value: UpdateRepoProfileResponse): Buffer =>
      Buffer.from(UpdateRepoProfileResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): UpdateRepoProfileResponse => UpdateRepoProfileResponse.decode(value),
  },
  deleteRepoProfile: {
    path: "/agentfleet.v1.DashboardService/DeleteRepoProfile" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DeleteRepoProfileRequest): Buffer =>
      Buffer.from(DeleteRepoProfileRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DeleteRepoProfileRequest => DeleteRepoProfileRequest.decode(value),
    responseSerialize: (value: DeleteRepoProfileResponse): Buffer =>
      Buffer.from(DeleteRepoProfileResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DeleteRepoProfileResponse => DeleteRepoProfileResponse.decode(value),
  },
  listPromptSnippets: {
    path: "/agentfleet.v1.DashboardService/ListPromptSnippets" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListPromptSnippetsRequest): Buffer =>
      Buffer.from(ListPromptSnippetsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListPromptSnippetsRequest => ListPromptSnippetsRequest.decode(value),
    responseSerialize: (value: ListPromptSnippetsResponse): Buffer =>
      Buffer.from(ListPromptSnippetsResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListPromptSnippetsResponse => ListPromptSnippetsResponse.decode(value),
  },
  createPromptSnippet: {
    path: "/agentfleet.v1.DashboardService/CreatePromptSnippet" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CreatePromptSnippetRequest): Buffer =>
      Buffer.from(CreatePromptSnippetRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CreatePromptSnippetRequest => CreatePromptSnippetRequest.decode(value),
    responseSerialize: (value: CreatePromptSnippetResponse): Buffer =>
      Buffer.from(CreatePromptSnippetResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CreatePromptSnippetResponse => CreatePromptSnippetResponse.decode(value),
  },
  updatePromptSnippet: {
    path: "/agentfleet.v1.DashboardService/UpdatePromptSnippet" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: UpdatePromptSnippetRequest): Buffer =>
      Buffer.from(UpdatePromptSnippetRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): UpdatePromptSnippetRequest => UpdatePromptSnippetRequest.decode(value),
    responseSerialize: (value: UpdatePromptSnippetResponse): Buffer =>
      Buffer.from(UpdatePromptSnippetResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): UpdatePromptSnippetResponse => UpdatePromptSnippetResponse.decode(value),
  },
  deletePromptSnippet: {
    path: "/agentfleet.v1.DashboardService/DeletePromptSnippet" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DeletePromptSnippetRequest): Buffer =>
      Buffer.from(DeletePromptSnippetRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DeletePromptSnippetRequest => DeletePromptSnippetRequest.decode(value),
    responseSerialize: (value: DeletePromptSnippetResponse): Buffer =>
      Buffer.from(DeletePromptSnippetResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DeletePromptSnippetResponse => DeletePromptSnippetResponse.decode(value),
  },
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  listFiles: {
    path: "/agentfleet.v1.DashboardService/ListFiles" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListFilesRequest): Buffer => Buffer.from(ListFilesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListFilesRequest => ListFilesRequest.decode(value),
    responseSerialize: (value: ListFilesResponse): Buffer => Buffer.from(ListFilesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListFilesResponse => ListFilesResponse.decode(value),
  },
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  getFileUploadUrl: {
    path: "/agentfleet.v1.DashboardService/GetFileUploadUrl" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetFileUploadUrlRequest): Buffer =>
      Buffer.from(GetFileUploadUrlRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetFileUploadUrlRequest => GetFileUploadUrlRequest.decode(value),
    responseSerialize: (value: GetFileUploadUrlResponse): Buffer =>
      Buffer.from(GetFileUploadUrlResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetFileUploadUrlResponse => GetFileUploadUrlResponse.decode(value),
  },
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  getFileDownloadUrl: {
    path: "/agentfleet.v1.DashboardService/GetFileDownloadUrl" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetFileDownloadUrlRequest): Buffer =>
      Buffer.from(GetFileDownloadUrlRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetFileDownloadUrlRequest => GetFileDownloadUrlRequest.decode(value),
    responseSerialize: (value: GetFileDownloadUrlResponse): Buffer =>
      Buffer.from(GetFileDownloadUrlResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetFileDownloadUrlResponse => GetFileDownloadUrlResponse.decode(value),
  },
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  deleteFile: {
    path: "/agentfleet.v1.DashboardService/DeleteFile" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DeleteFileRequest): Buffer => Buffer.from(DeleteFileRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DeleteFileRequest => DeleteFileRequest.decode(value),
    responseSerialize: (value: DeleteFileResponse): Buffer => Buffer.from(DeleteFileResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DeleteFileResponse => DeleteFileResponse.decode(value),
  },
  /**
   * Reuses core.proto's QueryLogsRequest/QueryLogsResponse
   * buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE
   */
  queryLogs: {
    path: "/agentfleet.v1.DashboardService/QueryLogs" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: QueryLogsRequest): Buffer => Buffer.from(QueryLogsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): QueryLogsRequest => QueryLogsRequest.decode(value),
    responseSerialize: (value: QueryLogsResponse): Buffer => Buffer.from(QueryLogsResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): QueryLogsResponse => QueryLogsResponse.decode(value),
  },
  /**
   * thot's activity feed + the human side of its permission prompts
   * (docs/adr/0035). The dashboard is the *only* place a thot permission
   * decision can be made — its Discord channel is notify-only and never
   * an approval path.
   */
  listThotEvents: {
    path: "/agentfleet.v1.DashboardService/ListThotEvents" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListThotEventsRequest): Buffer =>
      Buffer.from(ListThotEventsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListThotEventsRequest => ListThotEventsRequest.decode(value),
    responseSerialize: (value: ListThotEventsResponse): Buffer =>
      Buffer.from(ListThotEventsResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListThotEventsResponse => ListThotEventsResponse.decode(value),
  },
  respondToThotPermission: {
    path: "/agentfleet.v1.DashboardService/RespondToThotPermission" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RespondToThotPermissionRequest): Buffer =>
      Buffer.from(RespondToThotPermissionRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RespondToThotPermissionRequest => RespondToThotPermissionRequest.decode(value),
    responseSerialize: (value: RespondToThotPermissionResponse): Buffer =>
      Buffer.from(RespondToThotPermissionResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RespondToThotPermissionResponse =>
      RespondToThotPermissionResponse.decode(value),
  },
  /**
   * A human asking thot a question from the dashboard. ADR-0035 said thot
   * is reachable by "workers, alerts, and humans" — the sidecar covered
   * workers and the scheduler covered alerts, but humans could only ever
   * *answer* thot's prompts, never initiate. This closes that.
   *
   * core proxies to ThotService.AskThot rather than the browser calling
   * thot directly: the hub-and-spoke exception in ADR-0035 is for
   * in-cluster callers that need real-time reachability, and a browser is
   * neither. It also keeps thot's bearer token server-side.
   */
  askThot: {
    path: "/agentfleet.v1.DashboardService/AskThot" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DashboardServiceAskThotRequest): Buffer =>
      Buffer.from(DashboardServiceAskThotRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DashboardServiceAskThotRequest => DashboardServiceAskThotRequest.decode(value),
    responseSerialize: (value: DashboardServiceAskThotResponse): Buffer =>
      Buffer.from(DashboardServiceAskThotResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DashboardServiceAskThotResponse =>
      DashboardServiceAskThotResponse.decode(value),
  },
  /**
   * Dashboard-editable schedules (docs/adr/0035) — same "edit it in the
   * UI, no redeploy" shape ListRepos/CreateRepo established.
   */
  listScheduledAudits: {
    path: "/agentfleet.v1.DashboardService/ListScheduledAudits" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListScheduledAuditsRequest): Buffer =>
      Buffer.from(ListScheduledAuditsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListScheduledAuditsRequest => ListScheduledAuditsRequest.decode(value),
    responseSerialize: (value: ListScheduledAuditsResponse): Buffer =>
      Buffer.from(ListScheduledAuditsResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListScheduledAuditsResponse => ListScheduledAuditsResponse.decode(value),
  },
  createScheduledAudit: {
    path: "/agentfleet.v1.DashboardService/CreateScheduledAudit" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CreateScheduledAuditRequest): Buffer =>
      Buffer.from(CreateScheduledAuditRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CreateScheduledAuditRequest => CreateScheduledAuditRequest.decode(value),
    responseSerialize: (value: CreateScheduledAuditResponse): Buffer =>
      Buffer.from(CreateScheduledAuditResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CreateScheduledAuditResponse => CreateScheduledAuditResponse.decode(value),
  },
  updateScheduledAudit: {
    path: "/agentfleet.v1.DashboardService/UpdateScheduledAudit" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: UpdateScheduledAuditRequest): Buffer =>
      Buffer.from(UpdateScheduledAuditRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): UpdateScheduledAuditRequest => UpdateScheduledAuditRequest.decode(value),
    responseSerialize: (value: UpdateScheduledAuditResponse): Buffer =>
      Buffer.from(UpdateScheduledAuditResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): UpdateScheduledAuditResponse => UpdateScheduledAuditResponse.decode(value),
  },
  deleteScheduledAudit: {
    path: "/agentfleet.v1.DashboardService/DeleteScheduledAudit" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DeleteScheduledAuditRequest): Buffer =>
      Buffer.from(DeleteScheduledAuditRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DeleteScheduledAuditRequest => DeleteScheduledAuditRequest.decode(value),
    responseSerialize: (value: DeleteScheduledAuditResponse): Buffer =>
      Buffer.from(DeleteScheduledAuditResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DeleteScheduledAuditResponse => DeleteScheduledAuditResponse.decode(value),
  },
} as const;

export interface DashboardServiceServer extends UntypedServiceImplementation {
  listTasks: handleUnaryCall<ListTasksRequest, ListTasksResponse>;
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  getTask: handleUnaryCall<GetTaskRequest, GetTaskResponse>;
  createTask: handleUnaryCall<CreateTaskRequest, CreateTaskResponse>;
  /**
   * Reuses transcript.proto's ReadTranscriptSinceRequest/Response rather
   * than duplicating a byte-identical message pair.
   * buf:lint:ignore RPC_REQUEST_STANDARD_NAME
   * buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
   * buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE
   */
  getTranscript: handleUnaryCall<ReadTranscriptSinceRequest, ReadTranscriptSinceResponse>;
  /**
   * Streams transcript.proto's TranscriptEntry directly rather than
   * wrapping it in a redundant StreamTranscriptResponse.
   * buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
   * buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE
   */
  streamTranscript: handleServerStreamingCall<StreamTranscriptRequest, TranscriptEntry>;
  getE2EStatus: handleUnaryCall<GetE2eStatusRequest, GetE2eStatusResponse>;
  kill: handleUnaryCall<KillRequest, KillResponse>;
  interrupt: handleUnaryCall<InterruptRequest, InterruptResponse>;
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  setPermissionMode: handleUnaryCall<SetPermissionModeRequest, SetPermissionModeResponse>;
  warm: handleUnaryCall<WarmRequest, WarmResponse>;
  killE2E: handleUnaryCall<KillE2eRequest, KillE2eResponse>;
  answerQuestion: handleUnaryCall<AnswerQuestionRequest, AnswerQuestionResponse>;
  respondToPermission: handleUnaryCall<RespondToPermissionRequest, RespondToPermissionResponse>;
  discuss: handleUnaryCall<DiscussRequest, DiscussResponse>;
  deleteTask: handleUnaryCall<DeleteTaskRequest, DeleteTaskResponse>;
  /**
   * Reuses provisioner.proto's ListWorktreesRequest (identical shape,
   * empty) and DeleteWorktreeRequest/Response (identical shape, no
   * dashboard-specific fields needed) — same passthrough-reuse pattern
   * core.proto's ListE2eTools/CallE2eTool already established. The
   * response is enriched with a left-join against `tasks`, so it can't
   * reuse provisioner's own WorktreeInfo/ListWorktreesResponse as-is.
   * buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE
   * buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
   */
  listWorktrees: handleUnaryCall<ListWorktreesRequest, ListWorktreesViewResponse>;
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  deleteWorktree: handleUnaryCall<DeleteWorktreeRequest, DeleteWorktreeResponse>;
  getJournal: handleUnaryCall<GetJournalRequest, GetJournalResponse>;
  listRepos: handleUnaryCall<ListReposRequest, ListReposResponse>;
  createRepo: handleUnaryCall<CreateRepoRequest, CreateRepoResponse>;
  updateRepo: handleUnaryCall<UpdateRepoRequest, UpdateRepoResponse>;
  deleteRepo: handleUnaryCall<DeleteRepoRequest, DeleteRepoResponse>;
  listRepoProfiles: handleUnaryCall<ListRepoProfilesRequest, ListRepoProfilesResponse>;
  createRepoProfile: handleUnaryCall<CreateRepoProfileRequest, CreateRepoProfileResponse>;
  updateRepoProfile: handleUnaryCall<UpdateRepoProfileRequest, UpdateRepoProfileResponse>;
  deleteRepoProfile: handleUnaryCall<DeleteRepoProfileRequest, DeleteRepoProfileResponse>;
  listPromptSnippets: handleUnaryCall<ListPromptSnippetsRequest, ListPromptSnippetsResponse>;
  createPromptSnippet: handleUnaryCall<CreatePromptSnippetRequest, CreatePromptSnippetResponse>;
  updatePromptSnippet: handleUnaryCall<UpdatePromptSnippetRequest, UpdatePromptSnippetResponse>;
  deletePromptSnippet: handleUnaryCall<DeletePromptSnippetRequest, DeletePromptSnippetResponse>;
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  listFiles: handleUnaryCall<ListFilesRequest, ListFilesResponse>;
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  getFileUploadUrl: handleUnaryCall<GetFileUploadUrlRequest, GetFileUploadUrlResponse>;
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  getFileDownloadUrl: handleUnaryCall<GetFileDownloadUrlRequest, GetFileDownloadUrlResponse>;
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  deleteFile: handleUnaryCall<DeleteFileRequest, DeleteFileResponse>;
  /**
   * Reuses core.proto's QueryLogsRequest/QueryLogsResponse
   * buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE
   */
  queryLogs: handleUnaryCall<QueryLogsRequest, QueryLogsResponse>;
  /**
   * thot's activity feed + the human side of its permission prompts
   * (docs/adr/0035). The dashboard is the *only* place a thot permission
   * decision can be made — its Discord channel is notify-only and never
   * an approval path.
   */
  listThotEvents: handleUnaryCall<ListThotEventsRequest, ListThotEventsResponse>;
  respondToThotPermission: handleUnaryCall<RespondToThotPermissionRequest, RespondToThotPermissionResponse>;
  /**
   * A human asking thot a question from the dashboard. ADR-0035 said thot
   * is reachable by "workers, alerts, and humans" — the sidecar covered
   * workers and the scheduler covered alerts, but humans could only ever
   * *answer* thot's prompts, never initiate. This closes that.
   *
   * core proxies to ThotService.AskThot rather than the browser calling
   * thot directly: the hub-and-spoke exception in ADR-0035 is for
   * in-cluster callers that need real-time reachability, and a browser is
   * neither. It also keeps thot's bearer token server-side.
   */
  askThot: handleUnaryCall<DashboardServiceAskThotRequest, DashboardServiceAskThotResponse>;
  /**
   * Dashboard-editable schedules (docs/adr/0035) — same "edit it in the
   * UI, no redeploy" shape ListRepos/CreateRepo established.
   */
  listScheduledAudits: handleUnaryCall<ListScheduledAuditsRequest, ListScheduledAuditsResponse>;
  createScheduledAudit: handleUnaryCall<CreateScheduledAuditRequest, CreateScheduledAuditResponse>;
  updateScheduledAudit: handleUnaryCall<UpdateScheduledAuditRequest, UpdateScheduledAuditResponse>;
  deleteScheduledAudit: handleUnaryCall<DeleteScheduledAuditRequest, DeleteScheduledAuditResponse>;
}

export interface DashboardServiceClient extends Client {
  listTasks(
    request: ListTasksRequest,
    callback: (error: ServiceError | null, response: ListTasksResponse) => void,
  ): ClientUnaryCall;
  listTasks(
    request: ListTasksRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListTasksResponse) => void,
  ): ClientUnaryCall;
  listTasks(
    request: ListTasksRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListTasksResponse) => void,
  ): ClientUnaryCall;
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  getTask(
    request: GetTaskRequest,
    callback: (error: ServiceError | null, response: GetTaskResponse) => void,
  ): ClientUnaryCall;
  getTask(
    request: GetTaskRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetTaskResponse) => void,
  ): ClientUnaryCall;
  getTask(
    request: GetTaskRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetTaskResponse) => void,
  ): ClientUnaryCall;
  createTask(
    request: CreateTaskRequest,
    callback: (error: ServiceError | null, response: CreateTaskResponse) => void,
  ): ClientUnaryCall;
  createTask(
    request: CreateTaskRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CreateTaskResponse) => void,
  ): ClientUnaryCall;
  createTask(
    request: CreateTaskRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CreateTaskResponse) => void,
  ): ClientUnaryCall;
  /**
   * Reuses transcript.proto's ReadTranscriptSinceRequest/Response rather
   * than duplicating a byte-identical message pair.
   * buf:lint:ignore RPC_REQUEST_STANDARD_NAME
   * buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
   * buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE
   */
  getTranscript(
    request: ReadTranscriptSinceRequest,
    callback: (error: ServiceError | null, response: ReadTranscriptSinceResponse) => void,
  ): ClientUnaryCall;
  getTranscript(
    request: ReadTranscriptSinceRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ReadTranscriptSinceResponse) => void,
  ): ClientUnaryCall;
  getTranscript(
    request: ReadTranscriptSinceRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ReadTranscriptSinceResponse) => void,
  ): ClientUnaryCall;
  /**
   * Streams transcript.proto's TranscriptEntry directly rather than
   * wrapping it in a redundant StreamTranscriptResponse.
   * buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
   * buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE
   */
  streamTranscript(
    request: StreamTranscriptRequest,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<TranscriptEntry>;
  streamTranscript(
    request: StreamTranscriptRequest,
    metadata?: Metadata,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<TranscriptEntry>;
  getE2EStatus(
    request: GetE2eStatusRequest,
    callback: (error: ServiceError | null, response: GetE2eStatusResponse) => void,
  ): ClientUnaryCall;
  getE2EStatus(
    request: GetE2eStatusRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetE2eStatusResponse) => void,
  ): ClientUnaryCall;
  getE2EStatus(
    request: GetE2eStatusRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetE2eStatusResponse) => void,
  ): ClientUnaryCall;
  kill(request: KillRequest, callback: (error: ServiceError | null, response: KillResponse) => void): ClientUnaryCall;
  kill(
    request: KillRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: KillResponse) => void,
  ): ClientUnaryCall;
  kill(
    request: KillRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: KillResponse) => void,
  ): ClientUnaryCall;
  interrupt(
    request: InterruptRequest,
    callback: (error: ServiceError | null, response: InterruptResponse) => void,
  ): ClientUnaryCall;
  interrupt(
    request: InterruptRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: InterruptResponse) => void,
  ): ClientUnaryCall;
  interrupt(
    request: InterruptRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: InterruptResponse) => void,
  ): ClientUnaryCall;
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  setPermissionMode(
    request: SetPermissionModeRequest,
    callback: (error: ServiceError | null, response: SetPermissionModeResponse) => void,
  ): ClientUnaryCall;
  setPermissionMode(
    request: SetPermissionModeRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SetPermissionModeResponse) => void,
  ): ClientUnaryCall;
  setPermissionMode(
    request: SetPermissionModeRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SetPermissionModeResponse) => void,
  ): ClientUnaryCall;
  warm(request: WarmRequest, callback: (error: ServiceError | null, response: WarmResponse) => void): ClientUnaryCall;
  warm(
    request: WarmRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: WarmResponse) => void,
  ): ClientUnaryCall;
  warm(
    request: WarmRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: WarmResponse) => void,
  ): ClientUnaryCall;
  killE2E(
    request: KillE2eRequest,
    callback: (error: ServiceError | null, response: KillE2eResponse) => void,
  ): ClientUnaryCall;
  killE2E(
    request: KillE2eRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: KillE2eResponse) => void,
  ): ClientUnaryCall;
  killE2E(
    request: KillE2eRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: KillE2eResponse) => void,
  ): ClientUnaryCall;
  answerQuestion(
    request: AnswerQuestionRequest,
    callback: (error: ServiceError | null, response: AnswerQuestionResponse) => void,
  ): ClientUnaryCall;
  answerQuestion(
    request: AnswerQuestionRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: AnswerQuestionResponse) => void,
  ): ClientUnaryCall;
  answerQuestion(
    request: AnswerQuestionRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: AnswerQuestionResponse) => void,
  ): ClientUnaryCall;
  respondToPermission(
    request: RespondToPermissionRequest,
    callback: (error: ServiceError | null, response: RespondToPermissionResponse) => void,
  ): ClientUnaryCall;
  respondToPermission(
    request: RespondToPermissionRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RespondToPermissionResponse) => void,
  ): ClientUnaryCall;
  respondToPermission(
    request: RespondToPermissionRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RespondToPermissionResponse) => void,
  ): ClientUnaryCall;
  discuss(
    request: DiscussRequest,
    callback: (error: ServiceError | null, response: DiscussResponse) => void,
  ): ClientUnaryCall;
  discuss(
    request: DiscussRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DiscussResponse) => void,
  ): ClientUnaryCall;
  discuss(
    request: DiscussRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DiscussResponse) => void,
  ): ClientUnaryCall;
  deleteTask(
    request: DeleteTaskRequest,
    callback: (error: ServiceError | null, response: DeleteTaskResponse) => void,
  ): ClientUnaryCall;
  deleteTask(
    request: DeleteTaskRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DeleteTaskResponse) => void,
  ): ClientUnaryCall;
  deleteTask(
    request: DeleteTaskRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DeleteTaskResponse) => void,
  ): ClientUnaryCall;
  /**
   * Reuses provisioner.proto's ListWorktreesRequest (identical shape,
   * empty) and DeleteWorktreeRequest/Response (identical shape, no
   * dashboard-specific fields needed) — same passthrough-reuse pattern
   * core.proto's ListE2eTools/CallE2eTool already established. The
   * response is enriched with a left-join against `tasks`, so it can't
   * reuse provisioner's own WorktreeInfo/ListWorktreesResponse as-is.
   * buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE
   * buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
   */
  listWorktrees(
    request: ListWorktreesRequest,
    callback: (error: ServiceError | null, response: ListWorktreesViewResponse) => void,
  ): ClientUnaryCall;
  listWorktrees(
    request: ListWorktreesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListWorktreesViewResponse) => void,
  ): ClientUnaryCall;
  listWorktrees(
    request: ListWorktreesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListWorktreesViewResponse) => void,
  ): ClientUnaryCall;
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  deleteWorktree(
    request: DeleteWorktreeRequest,
    callback: (error: ServiceError | null, response: DeleteWorktreeResponse) => void,
  ): ClientUnaryCall;
  deleteWorktree(
    request: DeleteWorktreeRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DeleteWorktreeResponse) => void,
  ): ClientUnaryCall;
  deleteWorktree(
    request: DeleteWorktreeRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DeleteWorktreeResponse) => void,
  ): ClientUnaryCall;
  getJournal(
    request: GetJournalRequest,
    callback: (error: ServiceError | null, response: GetJournalResponse) => void,
  ): ClientUnaryCall;
  getJournal(
    request: GetJournalRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetJournalResponse) => void,
  ): ClientUnaryCall;
  getJournal(
    request: GetJournalRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetJournalResponse) => void,
  ): ClientUnaryCall;
  listRepos(
    request: ListReposRequest,
    callback: (error: ServiceError | null, response: ListReposResponse) => void,
  ): ClientUnaryCall;
  listRepos(
    request: ListReposRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListReposResponse) => void,
  ): ClientUnaryCall;
  listRepos(
    request: ListReposRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListReposResponse) => void,
  ): ClientUnaryCall;
  createRepo(
    request: CreateRepoRequest,
    callback: (error: ServiceError | null, response: CreateRepoResponse) => void,
  ): ClientUnaryCall;
  createRepo(
    request: CreateRepoRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CreateRepoResponse) => void,
  ): ClientUnaryCall;
  createRepo(
    request: CreateRepoRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CreateRepoResponse) => void,
  ): ClientUnaryCall;
  updateRepo(
    request: UpdateRepoRequest,
    callback: (error: ServiceError | null, response: UpdateRepoResponse) => void,
  ): ClientUnaryCall;
  updateRepo(
    request: UpdateRepoRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: UpdateRepoResponse) => void,
  ): ClientUnaryCall;
  updateRepo(
    request: UpdateRepoRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: UpdateRepoResponse) => void,
  ): ClientUnaryCall;
  deleteRepo(
    request: DeleteRepoRequest,
    callback: (error: ServiceError | null, response: DeleteRepoResponse) => void,
  ): ClientUnaryCall;
  deleteRepo(
    request: DeleteRepoRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DeleteRepoResponse) => void,
  ): ClientUnaryCall;
  deleteRepo(
    request: DeleteRepoRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DeleteRepoResponse) => void,
  ): ClientUnaryCall;
  listRepoProfiles(
    request: ListRepoProfilesRequest,
    callback: (error: ServiceError | null, response: ListRepoProfilesResponse) => void,
  ): ClientUnaryCall;
  listRepoProfiles(
    request: ListRepoProfilesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListRepoProfilesResponse) => void,
  ): ClientUnaryCall;
  listRepoProfiles(
    request: ListRepoProfilesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListRepoProfilesResponse) => void,
  ): ClientUnaryCall;
  createRepoProfile(
    request: CreateRepoProfileRequest,
    callback: (error: ServiceError | null, response: CreateRepoProfileResponse) => void,
  ): ClientUnaryCall;
  createRepoProfile(
    request: CreateRepoProfileRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CreateRepoProfileResponse) => void,
  ): ClientUnaryCall;
  createRepoProfile(
    request: CreateRepoProfileRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CreateRepoProfileResponse) => void,
  ): ClientUnaryCall;
  updateRepoProfile(
    request: UpdateRepoProfileRequest,
    callback: (error: ServiceError | null, response: UpdateRepoProfileResponse) => void,
  ): ClientUnaryCall;
  updateRepoProfile(
    request: UpdateRepoProfileRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: UpdateRepoProfileResponse) => void,
  ): ClientUnaryCall;
  updateRepoProfile(
    request: UpdateRepoProfileRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: UpdateRepoProfileResponse) => void,
  ): ClientUnaryCall;
  deleteRepoProfile(
    request: DeleteRepoProfileRequest,
    callback: (error: ServiceError | null, response: DeleteRepoProfileResponse) => void,
  ): ClientUnaryCall;
  deleteRepoProfile(
    request: DeleteRepoProfileRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DeleteRepoProfileResponse) => void,
  ): ClientUnaryCall;
  deleteRepoProfile(
    request: DeleteRepoProfileRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DeleteRepoProfileResponse) => void,
  ): ClientUnaryCall;
  listPromptSnippets(
    request: ListPromptSnippetsRequest,
    callback: (error: ServiceError | null, response: ListPromptSnippetsResponse) => void,
  ): ClientUnaryCall;
  listPromptSnippets(
    request: ListPromptSnippetsRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListPromptSnippetsResponse) => void,
  ): ClientUnaryCall;
  listPromptSnippets(
    request: ListPromptSnippetsRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListPromptSnippetsResponse) => void,
  ): ClientUnaryCall;
  createPromptSnippet(
    request: CreatePromptSnippetRequest,
    callback: (error: ServiceError | null, response: CreatePromptSnippetResponse) => void,
  ): ClientUnaryCall;
  createPromptSnippet(
    request: CreatePromptSnippetRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CreatePromptSnippetResponse) => void,
  ): ClientUnaryCall;
  createPromptSnippet(
    request: CreatePromptSnippetRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CreatePromptSnippetResponse) => void,
  ): ClientUnaryCall;
  updatePromptSnippet(
    request: UpdatePromptSnippetRequest,
    callback: (error: ServiceError | null, response: UpdatePromptSnippetResponse) => void,
  ): ClientUnaryCall;
  updatePromptSnippet(
    request: UpdatePromptSnippetRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: UpdatePromptSnippetResponse) => void,
  ): ClientUnaryCall;
  updatePromptSnippet(
    request: UpdatePromptSnippetRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: UpdatePromptSnippetResponse) => void,
  ): ClientUnaryCall;
  deletePromptSnippet(
    request: DeletePromptSnippetRequest,
    callback: (error: ServiceError | null, response: DeletePromptSnippetResponse) => void,
  ): ClientUnaryCall;
  deletePromptSnippet(
    request: DeletePromptSnippetRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DeletePromptSnippetResponse) => void,
  ): ClientUnaryCall;
  deletePromptSnippet(
    request: DeletePromptSnippetRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DeletePromptSnippetResponse) => void,
  ): ClientUnaryCall;
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  listFiles(
    request: ListFilesRequest,
    callback: (error: ServiceError | null, response: ListFilesResponse) => void,
  ): ClientUnaryCall;
  listFiles(
    request: ListFilesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListFilesResponse) => void,
  ): ClientUnaryCall;
  listFiles(
    request: ListFilesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListFilesResponse) => void,
  ): ClientUnaryCall;
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  getFileUploadUrl(
    request: GetFileUploadUrlRequest,
    callback: (error: ServiceError | null, response: GetFileUploadUrlResponse) => void,
  ): ClientUnaryCall;
  getFileUploadUrl(
    request: GetFileUploadUrlRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetFileUploadUrlResponse) => void,
  ): ClientUnaryCall;
  getFileUploadUrl(
    request: GetFileUploadUrlRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetFileUploadUrlResponse) => void,
  ): ClientUnaryCall;
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  getFileDownloadUrl(
    request: GetFileDownloadUrlRequest,
    callback: (error: ServiceError | null, response: GetFileDownloadUrlResponse) => void,
  ): ClientUnaryCall;
  getFileDownloadUrl(
    request: GetFileDownloadUrlRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetFileDownloadUrlResponse) => void,
  ): ClientUnaryCall;
  getFileDownloadUrl(
    request: GetFileDownloadUrlRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetFileDownloadUrlResponse) => void,
  ): ClientUnaryCall;
  /** buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE */
  deleteFile(
    request: DeleteFileRequest,
    callback: (error: ServiceError | null, response: DeleteFileResponse) => void,
  ): ClientUnaryCall;
  deleteFile(
    request: DeleteFileRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DeleteFileResponse) => void,
  ): ClientUnaryCall;
  deleteFile(
    request: DeleteFileRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DeleteFileResponse) => void,
  ): ClientUnaryCall;
  /**
   * Reuses core.proto's QueryLogsRequest/QueryLogsResponse
   * buf:lint:ignore RPC_REQUEST_RESPONSE_UNIQUE
   */
  queryLogs(
    request: QueryLogsRequest,
    callback: (error: ServiceError | null, response: QueryLogsResponse) => void,
  ): ClientUnaryCall;
  queryLogs(
    request: QueryLogsRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: QueryLogsResponse) => void,
  ): ClientUnaryCall;
  queryLogs(
    request: QueryLogsRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: QueryLogsResponse) => void,
  ): ClientUnaryCall;
  /**
   * thot's activity feed + the human side of its permission prompts
   * (docs/adr/0035). The dashboard is the *only* place a thot permission
   * decision can be made — its Discord channel is notify-only and never
   * an approval path.
   */
  listThotEvents(
    request: ListThotEventsRequest,
    callback: (error: ServiceError | null, response: ListThotEventsResponse) => void,
  ): ClientUnaryCall;
  listThotEvents(
    request: ListThotEventsRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListThotEventsResponse) => void,
  ): ClientUnaryCall;
  listThotEvents(
    request: ListThotEventsRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListThotEventsResponse) => void,
  ): ClientUnaryCall;
  respondToThotPermission(
    request: RespondToThotPermissionRequest,
    callback: (error: ServiceError | null, response: RespondToThotPermissionResponse) => void,
  ): ClientUnaryCall;
  respondToThotPermission(
    request: RespondToThotPermissionRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RespondToThotPermissionResponse) => void,
  ): ClientUnaryCall;
  respondToThotPermission(
    request: RespondToThotPermissionRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RespondToThotPermissionResponse) => void,
  ): ClientUnaryCall;
  /**
   * A human asking thot a question from the dashboard. ADR-0035 said thot
   * is reachable by "workers, alerts, and humans" — the sidecar covered
   * workers and the scheduler covered alerts, but humans could only ever
   * *answer* thot's prompts, never initiate. This closes that.
   *
   * core proxies to ThotService.AskThot rather than the browser calling
   * thot directly: the hub-and-spoke exception in ADR-0035 is for
   * in-cluster callers that need real-time reachability, and a browser is
   * neither. It also keeps thot's bearer token server-side.
   */
  askThot(
    request: DashboardServiceAskThotRequest,
    callback: (error: ServiceError | null, response: DashboardServiceAskThotResponse) => void,
  ): ClientUnaryCall;
  askThot(
    request: DashboardServiceAskThotRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DashboardServiceAskThotResponse) => void,
  ): ClientUnaryCall;
  askThot(
    request: DashboardServiceAskThotRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DashboardServiceAskThotResponse) => void,
  ): ClientUnaryCall;
  /**
   * Dashboard-editable schedules (docs/adr/0035) — same "edit it in the
   * UI, no redeploy" shape ListRepos/CreateRepo established.
   */
  listScheduledAudits(
    request: ListScheduledAuditsRequest,
    callback: (error: ServiceError | null, response: ListScheduledAuditsResponse) => void,
  ): ClientUnaryCall;
  listScheduledAudits(
    request: ListScheduledAuditsRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListScheduledAuditsResponse) => void,
  ): ClientUnaryCall;
  listScheduledAudits(
    request: ListScheduledAuditsRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListScheduledAuditsResponse) => void,
  ): ClientUnaryCall;
  createScheduledAudit(
    request: CreateScheduledAuditRequest,
    callback: (error: ServiceError | null, response: CreateScheduledAuditResponse) => void,
  ): ClientUnaryCall;
  createScheduledAudit(
    request: CreateScheduledAuditRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CreateScheduledAuditResponse) => void,
  ): ClientUnaryCall;
  createScheduledAudit(
    request: CreateScheduledAuditRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CreateScheduledAuditResponse) => void,
  ): ClientUnaryCall;
  updateScheduledAudit(
    request: UpdateScheduledAuditRequest,
    callback: (error: ServiceError | null, response: UpdateScheduledAuditResponse) => void,
  ): ClientUnaryCall;
  updateScheduledAudit(
    request: UpdateScheduledAuditRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: UpdateScheduledAuditResponse) => void,
  ): ClientUnaryCall;
  updateScheduledAudit(
    request: UpdateScheduledAuditRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: UpdateScheduledAuditResponse) => void,
  ): ClientUnaryCall;
  deleteScheduledAudit(
    request: DeleteScheduledAuditRequest,
    callback: (error: ServiceError | null, response: DeleteScheduledAuditResponse) => void,
  ): ClientUnaryCall;
  deleteScheduledAudit(
    request: DeleteScheduledAuditRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DeleteScheduledAuditResponse) => void,
  ): ClientUnaryCall;
  deleteScheduledAudit(
    request: DeleteScheduledAuditRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DeleteScheduledAuditResponse) => void,
  ): ClientUnaryCall;
}

export const DashboardServiceClient = makeGenericClientConstructor(
  DashboardServiceService,
  "agentfleet.v1.DashboardService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): DashboardServiceClient;
  service: typeof DashboardServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
