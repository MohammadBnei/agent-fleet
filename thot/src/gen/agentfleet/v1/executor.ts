// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: agentfleet/v1/executor.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "agentfleet.v1";

export interface ExecRequest {
  /**
   * argv, WITHOUT the leading "kubectl". An array, never a command
   * string: the executor spawns it directly, so shell metacharacters are
   * inert by construction rather than filtered. A string would put a
   * shell somewhere in this path, which is not a risk worth taking on the
   * fleet's most privileged process.
   */
  args: string[];
  /**
   * read_only selects the validated path.
   *
   * true  — for unattended callers (the kubectl_read tool bypasses
   *         canUseTool by design, so no human sees these). Rejected
   *         unless the verb is a known read verb and no credential or
   *         impersonation flags are present.
   * false — for human-gated calls. Deliberately a dumb pipe: the human
   *         already approved this exact argv through canUseTool, and a
   *         second allowlist here would only duplicate that gate while
   *         constraining what a human is permitted to decide. The
   *         ClusterRole remains the outer boundary either way.
   */
  readOnly: boolean;
}

export interface ExecResponse {
  stdout: string;
  stderr: string;
  exitCode: number;
  /**
   * Set when the request was refused before running anything (read_only
   * validation). Distinct from a non-zero exit_code, which means kubectl
   * itself ran and failed.
   */
  refused: string;
}

function createBaseExecRequest(): ExecRequest {
  return { args: [], readOnly: false };
}

export const ExecRequest: MessageFns<ExecRequest> = {
  encode(message: ExecRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.args) {
      writer.uint32(10).string(v!);
    }
    if (message.readOnly !== false) {
      writer.uint32(16).bool(message.readOnly);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.args.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.readOnly = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecRequest {
    return {
      args: globalThis.Array.isArray(object?.args) ? object.args.map((e: any) => globalThis.String(e)) : [],
      readOnly: isSet(object.readOnly)
        ? globalThis.Boolean(object.readOnly)
        : isSet(object.read_only)
        ? globalThis.Boolean(object.read_only)
        : false,
    };
  },

  toJSON(message: ExecRequest): unknown {
    const obj: any = {};
    if (message.args?.length) {
      obj.args = message.args;
    }
    if (message.readOnly !== false) {
      obj.readOnly = message.readOnly;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExecRequest>, I>>(base?: I): ExecRequest {
    return ExecRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecRequest>, I>>(object: I): ExecRequest {
    const message = createBaseExecRequest();
    message.args = object.args?.map((e) => e) || [];
    message.readOnly = object.readOnly ?? false;
    return message;
  },
};

function createBaseExecResponse(): ExecResponse {
  return { stdout: "", stderr: "", exitCode: 0, refused: "" };
}

export const ExecResponse: MessageFns<ExecResponse> = {
  encode(message: ExecResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stdout !== "") {
      writer.uint32(10).string(message.stdout);
    }
    if (message.stderr !== "") {
      writer.uint32(18).string(message.stderr);
    }
    if (message.exitCode !== 0) {
      writer.uint32(24).int32(message.exitCode);
    }
    if (message.refused !== "") {
      writer.uint32(34).string(message.refused);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stdout = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.stderr = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.exitCode = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.refused = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecResponse {
    return {
      stdout: isSet(object.stdout) ? globalThis.String(object.stdout) : "",
      stderr: isSet(object.stderr) ? globalThis.String(object.stderr) : "",
      exitCode: isSet(object.exitCode)
        ? globalThis.Number(object.exitCode)
        : isSet(object.exit_code)
        ? globalThis.Number(object.exit_code)
        : 0,
      refused: isSet(object.refused) ? globalThis.String(object.refused) : "",
    };
  },

  toJSON(message: ExecResponse): unknown {
    const obj: any = {};
    if (message.stdout !== "") {
      obj.stdout = message.stdout;
    }
    if (message.stderr !== "") {
      obj.stderr = message.stderr;
    }
    if (message.exitCode !== 0) {
      obj.exitCode = Math.round(message.exitCode);
    }
    if (message.refused !== "") {
      obj.refused = message.refused;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExecResponse>, I>>(base?: I): ExecResponse {
    return ExecResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExecResponse>, I>>(object: I): ExecResponse {
    const message = createBaseExecResponse();
    message.stdout = object.stdout ?? "";
    message.stderr = object.stderr ?? "";
    message.exitCode = object.exitCode ?? 0;
    message.refused = object.refused ?? "";
    return message;
  },
};

/**
 * ExecutorService is the single most privileged surface in the fleet: it
 * holds the cluster ClusterRole (infra-bootstrap gitops/platform/thot,
 * ADR-0032) and runs kubectl on behalf of pods that hold no RBAC at all.
 *
 * This is the ADR-0012 pattern applied to thot: the thing doing the
 * reasoning never holds infra-mutation credentials — a narrow service
 * does, and is reached over the network. It is what lets a thot session
 * be an ordinary worker pod (see ADR-0037, superseding ADR-0035).
 *
 * Every RPC requires `authorization: Bearer <THOT_AUTH_TOKEN>`.
 */
export type ExecutorServiceService = typeof ExecutorServiceService;
export const ExecutorServiceService = {
  exec: {
    path: "/agentfleet.v1.ExecutorService/Exec" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ExecRequest): Buffer => Buffer.from(ExecRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ExecRequest => ExecRequest.decode(value),
    responseSerialize: (value: ExecResponse): Buffer => Buffer.from(ExecResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ExecResponse => ExecResponse.decode(value),
  },
} as const;

export interface ExecutorServiceServer extends UntypedServiceImplementation {
  exec: handleUnaryCall<ExecRequest, ExecResponse>;
}

export interface ExecutorServiceClient extends Client {
  exec(request: ExecRequest, callback: (error: ServiceError | null, response: ExecResponse) => void): ClientUnaryCall;
  exec(
    request: ExecRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ExecResponse) => void,
  ): ClientUnaryCall;
  exec(
    request: ExecRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ExecResponse) => void,
  ): ClientUnaryCall;
}

export const ExecutorServiceClient = makeGenericClientConstructor(
  ExecutorServiceService,
  "agentfleet.v1.ExecutorService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): ExecutorServiceClient;
  service: typeof ExecutorServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
