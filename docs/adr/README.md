# Architecture Decision Records

One file per real decision — a competing alternative existed, a choice was
reversed, or a live incident forced a change. Format: Status / Date /
Context / Decision / Consequences. Status is one of `Proposed`, `Accepted`,
`Rejected`, `Superseded`.

Decisions with no real alternative ever on the table live in
[`../DECISIONS.md`](../DECISIONS.md) §1 instead, not here.

| ADR | Title | Status |
|---|---|---|
| [0001](0001-redis-durable-list-over-pubsub.md) | Durable Redis list over pub/sub for the planning transcript | Superseded by [0013](0013-go-fleet-core-and-e2e-provisioner-rewrite.md) |
| [0002](0002-no-orchestration-framework.md) | No orchestration framework (Hermes/OpenClaw rejected) | Superseded by [0017](0017-single-session-planning-pipeline.md) |
| [0003](0003-persistent-worker-pod-per-repo.md) | Persistent worker pod per target repo, not one Job per task | Superseded by [0019](0019-shared-pvc-and-unified-provisioner.md) |
| [0004](0004-subscription-oauth-not-metered-api.md) | Claude Code subscription OAuth, not a metered API key | Accepted |
| [0005](0005-explicit-approval-gate.md) | Explicit human approval only gates write/edit unlock | Superseded by [0029](0029-sessions-not-tasks-permission-prompt-not-approval-gate.md) — there is no prospective write/edit gate left to unlock; `/approve` is deleted, and `canUseTool` reproduces the SDK's own live per-tool-call permission prompt instead of a single fleet-imposed unlock event. "Never inferred from silence or sentiment" (this ADR's core constraint) still holds — a permission decision is still a real, structured RespondToPermission call, never inferred |
| [0006](0006-git-identity-from-bot-account.md) | Git commit identity derived live from the authenticated bot account | Accepted |
| [0007](0007-two-source-argocd-application.md) | Two-source ArgoCD Application for independent deploy-tag bumps | Accepted |
| [0008](0008-unbounded-guardrail-defaults.md) | Guardrail defaults are unbounded, capped only opt-in | Accepted (round-cap/checkpoint machinery, previously corrected by [0021](0021-continuous-streaming-session.md), deleted outright by [0025](0025-continuous-session-worker-redesign.md) — no round cap of any kind remains; the unbounded-by-default philosophy for `MAX_TURNS`/cost caps is unchanged) |
| [0009](0009-rtk-ponytail-baked-into-worker-image.md) | `rtk` + `ponytail` baked into the worker image | Accepted |
| [0010](0010-shared-rwx-pvc-across-apps.md) | Shared `ReadWriteMany` PVC across bot + both workers | Accepted |
| [0011](0011-critic-opt-out-and-context-handoff.md) | Critic session is opt-out (human-only), plus proposer→critic context handoff | Superseded by [0017](0017-single-session-planning-pipeline.md) |
| [0012](0012-e2e-provisioner-standalone-app.md) | On-demand e2e test environments via a standalone e2e-provisioner | Accepted (scope broadened to worker-pod provisioning by [0019](0019-shared-pvc-and-unified-provisioner.md); direct worker MCP access superseded by [0020](0020-hub-and-spoke-grpc-worker-sidecar.md)) |
| [0013](0013-go-fleet-core-and-e2e-provisioner-rewrite.md) | Go `fleet-core` replaces Redis coordination; `e2e-provisioner`/`bot` rewritten in Go | Accepted (two lines on gRPC/connect-es superseded by [0015](0015-connectrpc-dashboard-api.md); agent-facing MCP-over-HTTP superseded by [0020](0020-hub-and-spoke-grpc-worker-sidecar.md)) |
| [0014](0014-fleet-core-dashboard-backend.md) | fleet-core as the web dashboard's backend | Accepted |
| [0015](0015-connectrpc-dashboard-api.md) | ConnectRPC replaces the dashboard's REST+SSE API | Accepted |
| [0016](0016-task-crash-recovery-and-retry.md) | Task crash recovery, heartbeat reclaim, and transient-error retry | Superseded by [0048](0048-one-session-one-pod-one-shared-home.md) |
| [0017](0017-single-session-planning-pipeline.md) | Single-session planner replaces proposer/critic | Accepted (Discord-as-interactive-user section superseded by [0018](0018-ask-user-question-via-dashboard.md)) |
| [0018](0018-ask-user-question-via-dashboard.md) | AskUserQuestion is real, answered via the dashboard, not Discord | Superseded by [0025](0025-continuous-session-worker-redesign.md) — question-seq correlation added (the "no correlation ID needed" argument here was the actual gap), and Discord can now answer questions too, contradicting this ADR's dashboard-only framing |
| [0019](0019-shared-pvc-and-unified-provisioner.md) | Shared worktree PVC and a unified provisioner replace per-repo persistent workers | Accepted |
| [0020](0020-hub-and-spoke-grpc-worker-sidecar.md) | Hub-and-spoke gRPC between fleet-core/provisioner/worker sidecar; MCP kept local to the agent | Accepted (sidecar's human-message responsibility detailed by [0021](0021-continuous-streaming-session.md); point 4's e2e tool-call relay and point 6's MCP-locality superseded by [0045](0045-service-endpoint-roster-direct-dial.md) — points 1, 2, 3, 5 unchanged) |
| [0021](0021-continuous-streaming-session.md) | One continuous streaming-input session per task; live setPermissionMode() on approval | Superseded by [0029](0029-sessions-not-tasks-permission-prompt-not-approval-gate.md) — the `allowedTools`/`canUseTool` live-gate mechanism itself (not just the approve-signal trigger) is replaced by a generic per-tool-call prompt reproducing the SDK's own permission tiers; streaming-input-mode-per-session and `resume:`'s move from crash-recovery-only to general-purpose both still trace back to this ADR's spike |
| [0022](0022-batch-job-worker-pod-lifecycle.md) | Worker sessions run as `batch/v1.Job`, not hand-rolled bare Pods | Accepted |
| [0023](0023-worktree-reuse-and-branch-sweep.md) | Worktree/branch lifecycle redesigned around explicit signals only | Superseded by [0048](0048-one-session-one-pod-one-shared-home.md) |
| [0024](0024-crash-fast-path-and-journal-read.md) | Pod-crash fast-path detection, a real retry cap, and a `knowledge_journal` read path | Accepted |
| [0025](0025-continuous-session-worker-redesign.md) | Worker session is a plain Claude Code session — Discord/dashboard are transport only | Accepted (point 3, the approve-signal mechanism, superseded by [0029](0029-sessions-not-tasks-permission-prompt-not-approval-gate.md); points 1–2 (no round cap/phase boundary), 4 (question-seq correlation), and 5 (allowlist relay) are unchanged) |
| [0026](0026-core-stays-on-common-app-chart.md) | `core` stays on `common-app-chart`, no per-app Helm chart | Accepted |
| [0027](0027-arbitrary-permission-mode-and-command-palette.md) | Dashboard permission-mode selector (incl. `bypassPermissions`) and slash-command palette | Accepted (transport reused as-is by [0029](0029-sessions-not-tasks-permission-prompt-not-approval-gate.md); only the allowlist and what calls it changed. `allowDangerouslySkipPermissions` section superseded by [0052](0052-auto-mode-and-the-bypass-launch-profile.md), which adds `auto` to the selector and makes `bypassPermissions` a launch profile) |
| [0028](0028-dynamic-repo-config.md) | Dashboard-editable, DB-backed repo config | Accepted |
| [0029](0029-sessions-not-tasks-permission-prompt-not-approval-gate.md) | Sessions replace tasks as the durable unit; canUseTool becomes a live permission prompt, not a prospective approval gate | Accepted |
| [0030](0030-single-source-schema-via-golang-migrate.md) | golang-migrate replaces the hand-copied `db/schema.sql` + test-fixture schemas | Accepted |
| [0031](0031-garage-s3-shared-files.md) | Garage S3-backed shared file space, presigned URLs minted only by core | Accepted |
| [0032](0032-fleet-shared-pvc-directory.md) | PVC-resident, provisioner-synced fleet-shared skills/context — implements [0019](0019-shared-pvc-and-unified-provisioner.md) point 6 | Accepted |
| [0033](0033-journal-search-and-persistent-plugins.md) | Journal search and persistent plugins | Accepted |
| [0034](0034-environment-recipe-system.md) | Environment recipe system replaces the hardcoded e2e start-command switch — bounded tool/service ingredient catalog, three service scope modes, provisioner-minted deterministic per-task credentials | Superseded by [0048](0048-one-session-one-pod-one-shared-home.md) |
| [0035](0035-thot-cluster-agent.md) | `thot`, the fleet's cluster agent — a standing service with its own RBAC, stream and dashboard page | Superseded by 0037 |
| [0036](0036-e2e-recipe-visible-and-override-approved.md) | The e2e recipe is readable by the agent, its `start_cmd` override needs a human yes and stays ephemeral, and the app port gets a readiness probe | Superseded by [0048](0048-one-session-one-pod-one-shared-home.md) |
| [0037](0037-thot-is-a-worker-task.md) | `thot` is an ordinary worker task; cluster RBAC moves to a narrow `thot-executor`, and the hub-and-spoke exception is deleted | Accepted |
| [0038](0038-per-task-subdomain-e2e-preview.md) | Per-task subdomains for e2e previews — app served at `/`, one `*.e2e.bnei.dev` wildcard cert via DNS-01; supersedes 0012's path routing | Accepted |
| [0039](0039-e2e-pod-is-the-worker-sandbox.md) | The e2e pod is the worker's build/test sandbox — `run_command` registered statically and provisioned lazily, git deliberately kept out, `publishNotReadyAddresses` corrects [0036](0036-e2e-recipe-visible-and-override-approved.md)'s readiness probe, and the pod is resized for building (needing a namespace-wide `limitRange.max` raise) | Superseded by [0048](0048-one-session-one-pod-one-shared-home.md) |
| [0040](0040-session-liveness-and-stall-guard.md) | Session liveness (`working`/`blocked`/`idle`/`done`/`stalled`/`unknown`) is derived per read from inputs the activity decorator already writes, never a stored column; a pod that never reports activity is torn down after `STARTUP_STALL_MS` instead of holding a slot for ~40 min; turn-stall is informational only | Accepted |
| [0041](0041-inter-agent-prompting.md) | One session can list, prompt and wait on another — routed agent → own sidecar → core → target per [0020](0020-hub-and-spoke-grpc-worker-sidecar.md), addressed by task id, and fenced by guards (discussion-only delivery, refuse a `blocked` target, no self-prompt, depth cap). Folds in identity: a failed resume is reported instead of silently overwriting `session_id`, and `SaveSessionId` is lease-scoped | Accepted |
| [0042](0042-console-rewrite.md) | The dashboard becomes a console — list/detail as full-width alternates (the 320px sidebar deleted), a pending decision rendered and answerable *from the list*, the feed's twelve entry kinds ranked into five visually distinct tiers, one three-way density control with a deliberate desktop/mobile difference, a desktop decision spine and a mobile docked decision (spine superseded by [0043](0043-one-decision-surface.md)), two themes from the mockups' tokens, and cheap backend additions (worktree path/dirty/size, PVC meter, `RunScheduledAuditNow`, `RetryTask`, a `QueryLogs` caller) | Accepted |
| [0043](0043-one-decision-surface.md) | One decision surface: a pending permission/plan/question renders in a pinned `DecisionDock` between feed and composer, on **both** form factors, using the same cards the feed uses. Supersedes [0042](0042-console-rewrite.md) §5 — the desktop decision spine is deleted (`DENSITY → decisions` already was the zoom-out), along with the feed-inline rendering and the composer answer-chip row, so a decision is answerable from exactly one place. Mobile's hand-rolled dock (~115 lines) goes too, gaining multi-question/multi-select, real diffs and a denial reason | Accepted |
| [0044](0044-e2e-pod-outlives-the-app.md) | The e2e pod is a sandbox that may also run an app — PID 1 outlives every child (the app runs once, logged, never restarted), an app is optional, a terminal pod is not an existing session, the readiness probe stays unconditional so `app_ready` keeps telling the truth, waiting moves client-side into `run_command`'s backoff, sandboxes are GC'd, and which profile builds the sandbox becomes a per-repo column plus an approval-gated agent override | Superseded by [0048](0048-one-session-one-pod-one-shared-home.md) |
| [0045](0045-service-endpoint-roster-direct-dial.md) | The sidecar dials its task's sandbox directly, using a `ServiceEndpoint` roster carried on responses core already sends — no new RPC, no `services` table (Kubernetes stays ground truth), authorization by per-task NetworkPolicy rather than a token the sandbox would have to hold. Deletes `CallE2eTool`/`ListE2eTools` from both services and the whole `mcpproxy` package, so the provisioner stops speaking MCP. Supersedes [0020](0020-hub-and-spoke-grpc-worker-sidecar.md) point 4 (e2e relay only) and point 6. Measured first: the deleted hop cost **0–1 ms** of a 751 ms p50 tool call, so this is justified by coupling and deletion, explicitly **not** by latency | Superseded by [0048](0048-one-session-one-pod-one-shared-home.md) |
| [0046](0046-context-budget.md) | The fleet's context budget — cap every unbounded tool result at the source and never cap without returning a way to reach the rest (`run_command` tees the full log to `/tmp` and returns its path; `view_logs` points at its existing filter params rather than gaining a cursor), tune the SDK's env-only context knobs with `USE_API_CLEAR_TOOL_USES` specifically (the variant whose `exclude_tools` shape leaves MCP results clearable), and log `inputTokens` at last. Root cause recorded: microcompaction's tool set is hardcoded to built-ins, so [0039](0039-e2e-pod-is-the-worker-sandbox.md)'s routing of all builds through the `run_command` MCP tool opted the fleet out of it | Accepted |
| [0047](0047-metrics-scoped-to-the-hubs.md) | Prometheus metrics scoped to **`core` and the provisioner only** — worker/sidecar pods are single-shot Jobs that routinely live and die between two 30s scrapes, so a counter on them samples a random subset of sessions; their SDK turns/cost/tokens stay in Loki, where `session.ts` already logs them. Eight metrics, every one actually incremented, chosen for what Loki and kube-state-metrics can't already answer. Scraped by **ServiceMonitor, not `prometheus.io/*` annotations** — this cluster's Prometheus is the Operator, with no `additionalScrapeConfigs`, so annotations are inert. Adds an Observability console view whose topology is assembled from `tasks.pod_phase` and a PromQL proxy, so `core` gains **no cluster RBAC** ([0020](0020-hub-and-spoke-grpc-worker-sidecar.md) point 1 intact) | Accepted |
| [0048](0048-one-session-one-pod-one-shared-home.md) | One session, one pod, one shared home. A session is the unit and the **first message boots the pod** — no queue, no lease, no retry, no `status` (`done` never had a writer; the cap was implemented twice). Liveness reconciles against Kubernetes every 60s instead of a heartbeat. Proposals become their own table with no pod path, so "a machine may propose, never open" is the schema rather than a `SELECT` that politely skips a value. **The fleet leaves the git business** — no worktree, no branch, no sweep; the provisioner does one `git clone --shared` and the agent picks its own branch. **The sandbox merges into the worker**, deleting `execmcp`, `run_command`, the endpoint roster, the per-task NetworkPolicy, an image, a pod per session, and the three-table recipe system that stored what `cat package.json` answers. One PVC, four subPath mounts, **global** tool caches — the first time session isolation is a mount boundary rather than a directory-naming convention. Supersedes [0016](0016-task-crash-recovery-and-retry.md), [0023](0023-worktree-reuse-and-branch-sweep.md), [0034](0034-environment-recipe-system.md), [0036](0036-e2e-recipe-visible-and-override-approved.md), [0039](0039-e2e-pod-is-the-worker-sandbox.md), [0044](0044-e2e-pod-outlives-the-app.md), [0045](0045-service-endpoint-roster-direct-dial.md) | Accepted |
| [0049](0049-project-setting-source.md) | A session loads the **target repo's own** project settings — `settingSources: ["user", "project"]`, resolving [0032](0032-fleet-shared-pvc-directory.md)'s open question. `"project"` is what gates project `CLAUDE.md` discovery in the SDK, so until now a worker on `agent-fleet` never read `agent-fleet`'s CLAUDE.md, and this repo's own `.claude/skills/` were invisible to it. `"local"` stays out: `.claude/settings.local.json` is gitignored, so it would reach a session unreviewed. Because `"project"` also merges that repo's `permissions.allow` — which removes `canUseTool` from the path entirely — the fleet gains an `ask` block of its own (in `fleet-shared/settings.json` as decided here, relocated to `worker/src/session.ts`'s `FLEET_ASK_RULES` by [0052](0052-auto-mode-and-the-bypass-launch-profile.md)); the SDK's evaluator resolves **deny → ask → allow** and returns on the first match, so a user-scope `ask` outranks a project-scope `allow`, while still letting a human approve the `git push` that every result depends on. No specificity tiebreak, so `Bash(curl:*)` in `ask` swallowed the two localhost `curl` allow entries, which were deleted rather than left claiming a permission they no longer granted | Accepted (ask-block placement superseded by [0052](0052-auto-mode-and-the-bypass-launch-profile.md); `settingSources` unchanged) |
| [0050](0050-a-question-outlives-its-pod.md) | A blocking `AskUserQuestion` survives pod teardown and warm — it returns `{"status":"pending"}`, the turn ends, the pod is free to idle-reap, and the human's answer (a durable `answer` row) is delivered to the **next** pod on warm via the same `streamHumanMessages` replay every human message uses, fed in as a labeled input turn. `ReserveSlot`'s stale-close now spares `question` rows (closing only `permission_request`, whose allow/deny buttons are bound to a dead pod's `canUseTool` promise), because auto-answering the question on warm would destroy the exact answer the human was about to give. Mechanical, not a prompt loop — the tool description tells the model to end its turn, not re-invoke. No proto change, no `blocking` flag: `reuseOrAppendQuestion` + the fast `pending` return already give the model subsidiary-vs-blocking. Partially supersedes [0018](0018-ask-user-question-via-dashboard.md)'s in-pod poll model for the teardown case only | Accepted |
| [0051](0051-browsers-on-the-shared-pvc.md) | Playwright's **browser builds move to the shared PVC**, mounted read-only at `/ms-playwright` with `PLAYWRIGHT_BROWSERS_PATH` pointing at it; the apt system libs stay baked (`install-deps`, no download) because no mount can provide root-installed packages. Worker image **5.13 GB → 2.41 GB**, and the 2 GB lives once instead of in a layer on every node for every image version. A fixed-name Job on the **worker image** fills it at provisioner startup, running both of [0044](0044-e2e-pod-outlives-the-app.md)'s installs — those two packages bundle different `playwright-core` versions and resolve different build numbers, and the versions that decide which builds are correct live in that image's `node_modules`. Idempotent by construction, so it fires on every start with no marker and an image bump repairs the cache. Rejected: **per-repo image variants**, which make browsing a provisioning-time decision on a per-*repo* knob (built and measured at 1.87/5.13 GB before being thrown away), and a **separate browser pod**, which reopens [0044](0044-e2e-pod-outlives-the-app.md)'s cross-pod failures and cannot mount the `ReadWriteOnce` session tree. Amends [0044](0044-e2e-pod-outlives-the-app.md) and [0048](0048-one-session-one-pod-one-shared-home.md) §6 | Accepted |
| [0052](0052-auto-mode-and-the-bypass-launch-profile.md) | **`auto` is the working mode; `bypassPermissions` is a launch profile.** SDK 0.3.233 moved the permission mode's allow *below* ask rules and `requiresUserInteraction()`, and gates bypass on a value computed once at launch — so a mid-session switch into bypass is refused outright (and was being swallowed into a warn, then treated as an allow), and `permissions.ask` outranks bypass, leaving it unable to `gh pr create`. Plan approval now mirrors the CLI's own menu — "approve + auto mode" behind a confirm, `SetPermissionMode` **then** `RespondToPermission` — crossing the bypass boundary in either direction ends the pod and re-warms into the mode, a rejected switch is reported instead of logged, and [0049](0049-project-setting-source.md)'s ask list moves into `worker/src/session.ts` so it can be omitted for bypass. `allowDangerouslySkipPermissions` still never passed: re-verified that it would make plan mode auto-allow every write from turn one. Supersedes [0027](0027-arbitrary-permission-mode-and-command-palette.md)'s flag section | Accepted |
| [0053](0053-the-gate-is-canusetool-not-a-rule-list.md) | **The gate is `canUseTool`, not a list the SDK re-interprets.** Two live bugs one day after [0052](0052-auto-mode-and-the-bypass-launch-profile.md), which had shipped on a source read it explicitly declined to claim as verified: the fleet's own MCP tools prompted (needing permission before the agent could *ask* for permission), and `auto` still asked. Root cause read out of the shipped 0.3.233 binary — the `mcp__agent-fleet-sidecar__*` allow rule still matches, but two gates sit **above** the allow-rule lookup: a non-read-only MCP tool in `plan` mode (and `mcp-go` emits no `readOnlyHint`, so that is every sidecar tool in every planning session), and an org-level `effectiveMaxPermission` ceiling. `auto` asked because the SDK's classifier deliberately falls back to a human on an ask-rule match, and `FLEET_ASK_RULES` is eight entries wide. Both are the same shape: a decision the fleet had already made, expressed as configuration an evaluator it does not own re-interprets. Fixed by answering both in `canUseTool` — fleet-own tools always allowed, and `auto` allowing everything but `rm`/`sudo` — which makes the MCP wildcard and the classifier no longer load-bearing. `FLEET_ASK_RULES` becomes unconditional, and **`bypassPermissions` is deleted**: it bought only those two commands, and charged a launch profile (a pod re-warm on both boundary crossings, a per-pod ask list) for them. `auto` inherits its confirm; `db/migrations/000004` rewrites stored rows | Accepted |
| [0054](0054-the-toolchain-stays-in-the-pod.md) | **The toolchain stays in the pod; an OOM gets a name instead of a machine.** Moving the binaries and heavy compute into a separately-owned Proxmox LXC is rejected on a live survey, not on principle: there is no pod-to-LXC exec channel and no credential for one, the two obvious ways to build one are closed by accepted infra ADRs (no VPN mesh, no Infisical SSH CA) and a third keeps Proxmox out of GitOps, `mount`/`fuse` are `root@pam`-only, both hosts sit near 28 GB of 31 GB, and an LXC has neither Prometheus, Loki nor backup — while `build-runner`'s root-via-sudo buildah would make session isolation *worse* than one pod per session. Measured from inside a live pod: `memory.oom.group=1`, the agent costs 0.45 GB of the 4 GiB limit, the cgroup2 mount is read-only with nothing delegated. So an OOM kills the agent as collateral, **no limit value fixes it**, a child cgroup cannot be made from inside, and Linux has no per-process RSS limit — the only unit that owns a cgroup is a container. The reported pain is the silence, not the kill, so: report `OOMKilled` into the transcript (the Job now outlives its own GC), cap the runtimes' heaps to lower the odds, put a per-repo writable tools directory on the shared PVC per [0051](0051-browsers-on-the-shared-pvc.md) to kill the rebuild cycle without reviving [0048](0048-one-session-one-pod-one-shared-home.md) §6's deleted ingredient catalog, and only then consider a second container | Accepted |
| [0055](0055-the-journal-is-agent-knowledge-not-pod-telemetry.md) | **The journal is agent knowledge, not pod telemetry.** `journal_search` becomes `(repo?, query?, since?, until?, limit?)` — every argument optional, because the query the journal exists to answer ("every entry, all repos, last seven days") could not be expressed: `repo` and `query` were both required and no date bound existed, so [`editable-blog`'s weekly rundown curled `DashboardService/GetJournal` from a worker pod](https://github.com/MohammadBnei/agent-fleet/issues/198) and filtered client-side, reaching it only because `X-Fleet-Dashboard` is a CSRF guard rather than authorization (#200). The store was never the limit — `Search`/`List` already documented `repo == ""` as match-all. An omitted query drops the FTS predicate and reads the window newest-first, since relevance ranking answers "what matches my guess", not "what happened". The issue asked for an `eventType` filter to hide `pod.POD_PHASE_*`; tracing found the better fix — those rows were ~80% of the table, **nothing read them** (no dashboard view calls `GetJournal`, no session ever searched one), and the live value they duplicated is written by the same handler into `sessions.pod_phase` with the history in Loki. The same test then applied to `session.started`/`stopped`/`failed`, which were the remaining 96%: `started` carried nothing but its own `sessionId`, `stopped`'s "summary" was the last assistant text block already pushed to `transcript` verbatim, and only `failed`'s error string existed nowhere else — so that one **moves to the transcript** rather than dying with them. So nothing writes to the journal automatically any more, and the existing rows go by a **one-off manual `DELETE`, not a migration** — cleanup of a past mistake is not schema, and it keeps append-only literally true. Two decisions inside: newest-first (ascending + `LIMIT` truncates a window to its oldest rows) and a bare `YYYY-MM-DD` `until` covering that whole day (exclusive-at-midnight silently dropped today, and the branch's own test asserted the bug). Amends [0033](0033-journal-search-and-persistent-plugins.md) and [0024](0024-crash-fast-path-and-journal-read.md)'s journal-write half | Accepted |
| [0056](0056-the-console-gate-is-oidc-in-core.md) | **The console's gate is OIDC inside core, not forwardAuth at the ingress.** `fleet.bnei.dev` was gated by one shared `apr1` credential whose hash is in `k8s-cluster`'s git history, in front of agents holding repo write tokens and cluster RBAC, on the only `*.bnei.dev` host still grey at Cloudflare. [#209](https://github.com/MohammadBnei/agent-fleet/issues/209) and infra ADR-0039 both proposed forwardAuth; rejected **for this host** because a middleware gates the ingress and has no opinion about a caller that never reaches it — which is [#200](https://github.com/MohammadBnei/agent-fleet/issues/200) exactly, a worker pod POSTing to core's Service IP on the pod network. So core federates to authentik itself: state + nonce + PKCE, a `__Host-`prefixed signed cookie, a required `platform-admins` claim, and **fail-closed startup** because the Infisical operator renders empty Secrets often enough that "off when unset" would come up wide open looking healthy. Discovery is used rather than built paths, since authentik's `userinfo` is not under its issuer (that doubled path took ArgoCD down a day earlier). The CSRF header stays — it, not `SameSite`, is what stops a preview host forging a call, because `<id>-e2e.bnei.dev` is **same-site** with the console. `X-authentik-*` headers are deliberately never read: a pod-network caller can forge one, turning an authorization gap into impersonation. `basic-admin-auth` removed same-day once a real login was proven. **Two failures shipped on the way in and are recorded in Consequences**: the issuer was `TrimSuffix`ed (OIDC compares it byte-for-byte → 15-minute outage, no test exercised real discovery), and a variable shadowed the package, which broke the build only once both PRs were on main | Accepted |
| [0057](0057-coreservice-authenticates-with-the-session-lease.md) | **`CoreService` authenticates with the session's own `lease_id`.** Port 9090 authenticated *nobody*: any pod could call it, and every method naming a session took that name from the body on trust — [#200](https://github.com/MohammadBnei/agent-fleet/issues/200)'s wider half, and a surface entirely outside [0053](0053-the-gate-is-canusetool-not-a-rule-list.md)'s premise that an agent's authority is what `canUseTool` grants. The credential already existed: `ReserveSlot` mints it per warm, the provisioner injects it, `SaveAgentSessionID` already refused a stale one. It beats a purpose-built token on four counts — random rather than derived from a session id published in Discord links, per-pod rather than per-session-forever, rotating on every warm, and needing **no key handed to the provisioner**, which already holds `GH_TOKEN` and the only cluster RBAC. `ClearLease` at teardown makes it revocable rather than merely rotating. Authorization is an **explicit per-method table**, never a field-name rule: `PromptSession` carries `caller_session_id` *and* `target_session_id`, `SaveAgentSessionId` carries `session_id` *and* `agent_session_id`, `GetSession` calls it `id` — a generic lookup is wrong on all three, each time in the direction that grants authority. Unlisted methods fail closed, guarded by a test walking the proto descriptor. A **stream** interceptor too, since the server had only a unary chain and `StreamHumanMessages` is the live human-message feed. Journal and log reads stay deliberately fleet-wide, written down rather than quietly narrowed | Accepted |
| [0058](0058-an-answer-wakes-the-session.md) | **An answer wakes the session, and a permission's pod may not be reaped.** [0050](0050-a-question-outlives-its-pod.md) decided a question outlives its pod and the answer is delivered to the next pod on warm; the delivery half was never built, so it had never once worked. `resumeFromSeq` is `LatestSeq` = `MAX(seq)+1` read inside `WarmIfIdle`, so an answer appended to a cold session lands one BELOW the cursor of the pod warming is about to create — and the next warm's cursor is above it too, making such an entry **undeliverable forever**, not delivered late. `AnswerQuestion` now warms first like every other append that must reach an agent; `RespondToPermission` deliberately does not, because `ReserveSlot` stale-closes permission rows on warm and would auto-deny the request being answered. The idle sweep exempts unanswered **permissions only** — the first draft exempted every pending decision, which contradicts 0050 and would let one forgotten question hold 20% of a five-pod fleet. `AskUserQuestion`'s `timeoutMs` argument is deleted: an agent asked for 120000 and the call died at exactly 60000 (`DEFAULT_REQUEST_TIMEOUT_MSEC` in its own MCP client, invisible from the sidecar), the handler reported a failed tool call, and the agent pasted its four questions into the chat as prose. Fixed wait of 45s, and a cancelled wait returns the designed `pending` body rather than an error. The console's list fetched the **oldest** 1000 transcript entries (a zero limit is a forward read) while the detail view fetched the newest 200, so a long session's question was on the wire, counted by core, and invisible in the list — reported as "I can't see questions now". Decision UI is gated per decision **kind** rather than pod liveness, in both surfaces. Also corrects `PromptSession`'s claim that a capacity-rejected warm is harmless "since a later warm will pick it up from a cursor computed after it exists" — a cursor computed after the entry is above it, so those prompts were dropped permanently | Accepted |
