version: v2
plugins:
  - local: protoc-gen-go
    out: gen/go
    opt: paths=source_relative
  - local: protoc-gen-go-grpc
    out: gen/go
    opt: paths=source_relative
  - local: protoc-gen-connect-go
    out: gen/go
    opt: paths=source_relative
  # Connect-ES v2: protoc-gen-es alone generates both messages and service
  # descriptors (GenService, consumed directly by @connectrpc/connect's
  # createClient) — the separate protoc-gen-connect-es plugin is for the
  # older v1 API and produces an incompatible descriptor shape, so it's
  # deliberately not listed here.
  - local: ../dashboard/node_modules/.bin/protoc-gen-es
    out: ../dashboard/src/gen
    opt: target=ts
  # A protoc-gen-ts_proto block used to generate worker/src/gen. Deleted in
  # docs/adr/0048: nothing in worker/src ever imported it. The worker speaks
  # to its own pod's sidecar over plain HTTP/JSON on localhost and never
  # touches gRPC, so 8,692 lines of generated types existed only because CI
  # regenerated and auto-committed them on every proto change.
