// VENDORED from MohammadBnei/ukubi-stt (proto/stt/v1/stt.proto). Do not edit.
//
// It lives under core/ rather than in this repo's own proto/ module on purpose:
// proto/buf.gen.yaml runs protoc-gen-es over the WHOLE module into
// dashboard/src/gen, so dropping this file there would emit a stt_pb.ts that
// nothing imports — exactly the dead generated code ADR-0048 deleted 8,692
// lines of. core/buf.gen.yaml lists Go plugins only.
//

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: stt/v1/stt.proto

package sttv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	Stt_Recognize_FullMethodName = "/stt.v1.Stt/Recognize"
)

// SttClient is the client API for Stt service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// GPU speech-to-text. ADR-0044.
//
// Phase 1 is ONE unary RPC, deliberately. Browsers cannot do gRPC client- or
// bidirectional streaming — gRPC-Web and Connect give unary and
// server-streaming only, and `duplex: 'full'` fetch ships in no stable browser
// as of 2026. So realtime is chunked unary at utterance granularity: one
// transport, one auth path, no partials.
//
// There is STILL only one RPC, and that is the point: realtime is the same
// unary call carrying a session_id. Browsers cannot stream *up* under any
// transport gRPC offers, so the audio goes as discrete requests regardless —
// which means a streaming RPC would buy nothing and cost a second code path.
// ADR-0046 Decision 3.
type SttClient interface {
	Recognize(ctx context.Context, in *RecognizeRequest, opts ...grpc.CallOption) (*RecognizeResponse, error)
}

type sttClient struct {
	cc grpc.ClientConnInterface
}

func NewSttClient(cc grpc.ClientConnInterface) SttClient {
	return &sttClient{cc}
}

func (c *sttClient) Recognize(ctx context.Context, in *RecognizeRequest, opts ...grpc.CallOption) (*RecognizeResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RecognizeResponse)
	err := c.cc.Invoke(ctx, Stt_Recognize_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// SttServer is the server API for Stt service.
// All implementations must embed UnimplementedSttServer
// for forward compatibility.
//
// GPU speech-to-text. ADR-0044.
//
// Phase 1 is ONE unary RPC, deliberately. Browsers cannot do gRPC client- or
// bidirectional streaming — gRPC-Web and Connect give unary and
// server-streaming only, and `duplex: 'full'` fetch ships in no stable browser
// as of 2026. So realtime is chunked unary at utterance granularity: one
// transport, one auth path, no partials.
//
// There is STILL only one RPC, and that is the point: realtime is the same
// unary call carrying a session_id. Browsers cannot stream *up* under any
// transport gRPC offers, so the audio goes as discrete requests regardless —
// which means a streaming RPC would buy nothing and cost a second code path.
// ADR-0046 Decision 3.
type SttServer interface {
	Recognize(context.Context, *RecognizeRequest) (*RecognizeResponse, error)
	mustEmbedUnimplementedSttServer()
}

// UnimplementedSttServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedSttServer struct{}

func (UnimplementedSttServer) Recognize(context.Context, *RecognizeRequest) (*RecognizeResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Recognize not implemented")
}
func (UnimplementedSttServer) mustEmbedUnimplementedSttServer() {}
func (UnimplementedSttServer) testEmbeddedByValue()             {}

// UnsafeSttServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to SttServer will
// result in compilation errors.
type UnsafeSttServer interface {
	mustEmbedUnimplementedSttServer()
}

func RegisterSttServer(s grpc.ServiceRegistrar, srv SttServer) {
	// If the following call panics, it indicates UnimplementedSttServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&Stt_ServiceDesc, srv)
}

func _Stt_Recognize_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RecognizeRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(SttServer).Recognize(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: Stt_Recognize_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(SttServer).Recognize(ctx, req.(*RecognizeRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// Stt_ServiceDesc is the grpc.ServiceDesc for Stt service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var Stt_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "stt.v1.Stt",
	HandlerType: (*SttServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Recognize",
			Handler:    _Stt_Recognize_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "stt/v1/stt.proto",
}
